<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>JavaScript(七) 面向对象</title>
    <url>/posts/23b66728/</url>
    <content><![CDATA[<link rel="stylesheet" class="aplayer-secondary-style-marker" href="/assets/css/APlayer.min.css"><script src="/assets/js/APlayer.min.js" class="aplayer-secondary-script-marker"></script><p>JavaScript 支持面向对象的编程范式，面向对象是一种对现实世界理解和抽象的方法</p>
<p>所谓对象通常是指客观世界中的真实存在的事物，实物，实象。例如：一支笔、一本书、一只鸟</p>
<p><strong>面向对象编程的三大特征：</strong></p>
<ul>
<li><p><strong>封装</strong>：只关心入口和出口，而不关心过程</p>
</li>
<li><p><strong>继承</strong>：指类与类之间的关系。如果两个类都有相同的属性或方法，那么可以让一个类继承于另类，这样就不需要在前者再次定义同样的属性或方法</p>
</li>
<li><p><strong>多态</strong>：不同的对象可以定义具有相同名称的方法，方法是作用于所在的对象中。这种不同对象通过相同方法的调用实现各自行为的能力，被称之为多态</p>
</li>
</ul>
<h2 id="构造函数"><a href="#构造函数" class="headerlink" title="构造函数"></a>构造函数</h2><p>构造函数又可称为对象模版或类型对象，通过构造函数我们可以创建特定<strong>类型</strong>的对象( 实例 )</p>
<p>构造函数和普通函数的定义方式是一样的，通常为了区分，构造函数的名称首字母要求大写</p>
<p>语法：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> 名称(<span class="params">[形参<span class="number">1</span>, 形参<span class="number">2.</span>..形参N]</span>)</span>&#123;</span><br><span class="line">	<span class="comment">//constructor body</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="创建实例"><a href="#创建实例" class="headerlink" title="创建实例"></a>创建实例</h3><p>语法：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> 对象名 = <span class="keyword">new</span> 构造函数([实参<span class="number">1</span>, 实参<span class="number">2.</span>..实参N])</span><br></pre></td></tr></table></figure>

<p>例：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">/* 定义类 */</span></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">Person</span>(<span class="params">name, age, gender</span>)</span>&#123;</span><br><span class="line">  <span class="built_in">this</span>.name = name,</span><br><span class="line">  <span class="built_in">this</span>.age = age,</span><br><span class="line">  <span class="built_in">this</span>.gender = gender;</span><br><span class="line">&#125;</span><br><span class="line">Person.prototype.sayName = <span class="function"><span class="keyword">function</span>(<span class="params"></span>)</span>&#123; <span class="comment">//方法需要添加到原型中以节省资源</span></span><br><span class="line">  alert(<span class="built_in">this</span>.name)</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">/* 创建实例 */</span></span><br><span class="line"><span class="keyword">var</span> xiaoming = <span class="keyword">new</span> Person(<span class="string">&quot;小明&quot;</span>, <span class="number">18</span>, <span class="string">&quot;男&quot;</span>);</span><br><span class="line"><span class="keyword">var</span> xiaomei = <span class="keyword">new</span> Person(<span class="string">&quot;小美&quot;</span>, <span class="number">16</span>, <span class="string">&quot;女&quot;</span>);</span><br><span class="line"><span class="comment">/* 输出实例 */</span></span><br><span class="line"><span class="built_in">console</span>.log(xiaoming, xiaomei) <span class="comment">//输出：Person&#123;name: &#x27;小明&#x27;, ...&#125; Person&#123;name: &#x27;小美&#x27;, ...&#125;</span></span><br></pre></td></tr></table></figure>

<blockquote>
<h3 id="类"><a href="#类" class="headerlink" title="类"></a>类</h3><p>类是<strong>封装</strong>对象的属性和方法的<strong>载体</strong>。类定义了对象的特征，例如一辆汽车( 对象 )的名称、车型、重量、颜色等等都是这辆车的属性，而汽车的启动和停止则是这辆车的行为(方法)</p>
<p>类是面向对象编程中的一个基本的概念。实际上在 Js 中并没有类的概念❕，但 Js 通过构造函数和原型可以实现对类的支持</p>
<h3 id="实例"><a href="#实例" class="headerlink" title="实例"></a>实例</h3><p><em>类是对象的抽象，而对象是类的具体实例</em></p>
<p>通过类所创建的对象称为该类的实例，例如上面案例中的对象<code>xiaoming</code>和<code>xiaomei</code>都是<code>Person</code>类的实例</p>
<h3 id="this-关键字"><a href="#this-关键字" class="headerlink" title="this 关键字"></a>this 关键字</h3><p>this是Js引擎在执行时自动定义的变量，其值会指向一个对象</p>
<ul>
<li><p>在全局作用域中访问this时，其值指向全局对象<code>window</code>(默认值)</p>
</li>
<li><p>在函数作用域中访问this时，根据 <strong>函数调用方式</strong> 的不同，this会指向不同的对象</p>
</li>
</ul>
<ol>
<li><p>以方法调用时，this会指向该方法所在的对象</p>
</li>
<li><p>以构造函数调用( <code>new 构造函数</code> )时，this会指向其所创建的实例对象</p>
</li>
</ol>
<p>例：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> xiaoming = &#123;</span><br><span class="line">  <span class="attr">name</span>: <span class="string">&#x27;小明&#x27;</span></span><br><span class="line">&#125;</span><br><span class="line">xiaoming.sayName = <span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">  <span class="built_in">console</span>.log(<span class="string">&#x27;myName is &#x27;</span>+<span class="built_in">this</span>.name);</span><br><span class="line">&#125;</span><br><span class="line">xiaoming.sayName(); <span class="comment">//执行结果：myName is 小明</span></span><br></pre></td></tr></table></figure>

<h3 id="new-运算符"><a href="#new-运算符" class="headerlink" title="new 运算符"></a>new 运算符</h3><p>使用new运算符来进行<strong>对象实例化</strong>（根据类创建对象的过程）</p>
<p>实例化的过程：</p>
<ol>
<li>创建一个空对象（即<code>&#123;&#125;</code>）</li>
<li>执行构造函数，执行前会先</li>
</ol>
<ul>
<li>将变量<code>this</code>的值指向步骤1中所创建的那个对象</li>
<li>并向对象中添加属性<code>__proto__</code>，将该属性的值指向构造函数的<strong>原型对象</strong>（<code>prototype</code>）</li>
</ul>
<ol start="3">
<li>构造函数执行完后，返回<strong>实例对象</strong>( 没有手动返回了一个对象的情况下，为步骤1中所创建的那个对象 )</li>
</ol>
</blockquote>
<h2 id="call、apply、bind-方法"><a href="#call、apply、bind-方法" class="headerlink" title="call、apply、bind 方法"></a>call、apply、bind 方法</h2><p><code>call</code>、<code>apply</code>、<code>bind</code> 是 <strong>Function类型对象</strong>( 函数 )中的方法，通过 <code>call()</code>、<code>apply()</code>、<code>bind()</code> 方法可以改变函数作用域中变量 this 的值</p>
<p><code>call()</code>的语法：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">函数.call(<span class="built_in">this</span>的值, 实参<span class="number">1</span>, 实参<span class="number">2.</span>..实参N)</span><br></pre></td></tr></table></figure>

<p><code>apply()</code>的语法：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">函数.apply(<span class="built_in">this</span>的值, [实参列表])</span><br></pre></td></tr></table></figure>

<p><code>bind()</code>的语法：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">函数.bind(<span class="built_in">this</span>的值, 实参<span class="number">1</span>, 实参<span class="number">2.</span>..实参N)</span><br></pre></td></tr></table></figure>

<p>例：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">/* 例1 */</span></span><br><span class="line"><span class="built_in">window</span>.number = <span class="string">&#x27;one&#x27;</span>;</span><br><span class="line"><span class="keyword">var</span> s1 = &#123;<span class="attr">number</span>: <span class="string">&#x27;tow&#x27;</span>&#125;;</span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">change</span>(<span class="params"></span>)</span>&#123;</span><br><span class="line">	<span class="built_in">console</span>.log(<span class="built_in">this</span>.number);</span><br><span class="line">&#125;</span><br><span class="line">change.apply();         <span class="comment">//执行结果：one</span></span><br><span class="line">change.apply(<span class="built_in">window</span>);   <span class="comment">//执行结果：one</span></span><br><span class="line">change.apply(<span class="built_in">this</span>);     <span class="comment">//执行结果：one</span></span><br><span class="line">change.apply(s1);       <span class="comment">//执行结果：tow</span></span><br><span class="line"></span><br><span class="line"><span class="comment">/* 例2 */</span></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">greet</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">  <span class="keyword">var</span> reply = <span class="built_in">this</span>.animal + <span class="string">&#x27;通常只睡&#x27;</span> + <span class="built_in">this</span>.sleepDuration;</span><br><span class="line">  <span class="built_in">console</span>.log(reply);</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">var</span> obj = &#123;</span><br><span class="line">  <span class="attr">animal</span>: <span class="string">&#x27;猫&#x27;</span>,</span><br><span class="line">  <span class="attr">sleepDuration</span>: <span class="string">&#x27;12～16个小时&#x27;</span></span><br><span class="line">&#125;;</span><br><span class="line">greet.call(obj);</span><br></pre></td></tr></table></figure>

<blockquote>
<h3 id="call、apply、bind-的区别"><a href="#call、apply、bind-的区别" class="headerlink" title="call、apply、bind 的区别"></a>call、apply、bind 的区别</h3><p><code>call</code>、<code>apply</code>、<code>bind</code> 方法都包含一个<strong>对象</strong>( 严格模式下除外 )作为该方法的第1个参数，其后的参数会作为函数的实参。但在传参时，<code>apply()</code> 方法需要将实参以数组的形式传递至函数</p>
<p><code>call</code> 和 <code>apply</code> 方法会以指定的 this 的值调用一次该函数，而 <code>bind()</code> 方法会将其所在函数返回，不会自动调用</p>
<p>如果以上传入的第一个参数为 null 或 undefined 则不会改变函数作用域中 this 的值( 严格模式除外 )</p>
<p>例：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">add</span>(<span class="params">c, d</span>)</span>&#123;</span><br><span class="line"><span class="keyword">return</span> <span class="built_in">this</span>.a + <span class="built_in">this</span>.b + c + d;</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">var</span> s = &#123;<span class="attr">a</span>:<span class="number">1</span>, <span class="attr">b</span>:<span class="number">2</span>&#125;;</span><br><span class="line"><span class="built_in">console</span>.log(add.call(s, <span class="number">3</span>, <span class="number">4</span>)); <span class="comment">//输出：10</span></span><br><span class="line"><span class="built_in">console</span>.log(add.apply(s, [<span class="number">5</span>, <span class="number">6</span>])); <span class="comment">//输出：14</span></span><br></pre></td></tr></table></figure>
</blockquote>
<h2 id="原型（prototype）"><a href="#原型（prototype）" class="headerlink" title="原型（prototype）"></a>原型（prototype）</h2><p>函数是一个包含属性和方法的 <strong>Function 类型</strong>的对象。而原型( <code>prototype</code> )就是 Function 类型对象的一个属性</p>
<p>js 中所有函数在定义时，js 引擎都会自动向函数中添加<code>prototype</code>属性，其值是一个 Object 类型的对象( <code>new Object()</code> )，称为原型对象( prototype对象 )，该对象有一个 constructor 属性指向该构造函数</p>
<p>当通过构造函数创建实例时，实例对象中会有一个隐藏的内部属性( <code>__proto__</code> )指向其构造函数的原型对象</p>
<p>例：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> obj = <span class="keyword">new</span> <span class="built_in">Object</span>();</span><br><span class="line"><span class="built_in">console</span>.log(obj.__proto__ === <span class="built_in">Object</span>.prototype); <span class="comment">//输出：true</span></span><br></pre></td></tr></table></figure>

<blockquote>
<h3 id="原型链"><a href="#原型链" class="headerlink" title="原型链"></a>原型链</h3><p>每个实例对象都有一个私有属性（称之为<code>__proto__</code>）指向它的构造函数的原型对象（<code>prototype</code>）。该原型对象也有一个自己的原型对象（<code>__proto__</code>），层层向上直到一个对象的<code>__proto__</code>属性值为<code>null</code>为止。这种沿着<code>__proto__</code>属性的链式结构就称为原型链</p>
<p>例：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">Fn</span>(<span class="params"></span>)</span>&#123;</span><br><span class="line">	<span class="comment">//this.xxx = xxx;</span></span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">var</span> fun = <span class="keyword">new</span> Fn();</span><br><span class="line">Fn.prototype.a = <span class="number">123</span>;</span><br><span class="line"><span class="built_in">console</span>.log(fun.a, fun.b); <span class="comment">//输出：123 undefined</span></span><br></pre></td></tr></table></figure>

<p><img src="https://cdn3.tianli0.top/gh/k34869/images/image/20221112104157" alt="prototype"></p>
<p>原型对象就相当于一个公共的区域，所有同一个类的实例都可以访问到这个原型对象</p>
<p>当从对象中访问一个属性时会先在自身对象中查找</p>
<ul>
<li>如果没找到，则沿着<code>__proto__</code>这条链向上查找</li>
<li>如果依然没找到，则返回 undefined</li>
</ul>
<h3 id="Object-getPrototypeOf"><a href="#Object-getPrototypeOf" class="headerlink" title="Object.getPrototypeOf()"></a>Object.getPrototypeOf()</h3><p>描述：获取指定对象的原型</p>
<p>语法：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="built_in">Object</span>.getPrototypeOf(对象)</span><br></pre></td></tr></table></figure>

<h3 id="Object-setPrototypeOf"><a href="#Object-setPrototypeOf" class="headerlink" title="Object.setPrototypeOf()"></a>Object.setPrototypeOf()</h3><p>描述：设置指定对象的原型</p>
<p>语法：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="built_in">Object</span>.setPrototypeOf(对象, 原型)</span><br></pre></td></tr></table></figure>

<h3 id="hasOwnProperty-NaN"><a href="#hasOwnProperty-NaN" class="headerlink" title="hasOwnProperty()"></a>hasOwnProperty()</h3><p>描述：检测对象中的属性是否存在( 不包括原型中的属性，检测原型中的属性可以用 in 运算符 )，存在则返回 true，反之则为 false</p>
<p>语法：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">对象.hasOwnProperty(属性名) <span class="comment">//属性名为一个字符串</span></span><br></pre></td></tr></table></figure>
</blockquote>
<h2 id="继承"><a href="#继承" class="headerlink" title="继承"></a>继承</h2><p>JavaScript 是基于原型实现的继承，和其它传统的基于类的语言( 如 Java 或 C++ )相比，Js 显得比较独特</p>
<h3 id="原型链继承"><a href="#原型链继承" class="headerlink" title="原型链继承"></a>原型链继承</h3><p>核心：将父类实例作为子类原型</p>
<p>例：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">/* 定义A类 */</span></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">A</span>(<span class="params"></span>)</span>&#123;</span><br><span class="line">	<span class="built_in">this</span>.a = <span class="string">&#x27;My is a&#x27;</span></span><br><span class="line">&#125;</span><br><span class="line">A.prototype.ca = <span class="function"><span class="keyword">function</span>(<span class="params"></span>)</span>&#123;</span><br><span class="line">	alert(<span class="built_in">this</span>.a)</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">/* 定义B类 */</span></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">B</span>(<span class="params"></span>)</span>&#123;</span><br><span class="line">	<span class="built_in">this</span>.b = <span class="string">&#x27;My is b&#x27;</span></span><br><span class="line">&#125;</span><br><span class="line">B.prototype = <span class="keyword">new</span> A(); <span class="comment">//子类的prototype指向父类的实例对象</span></span><br><span class="line">B.prototype.constructor = B; <span class="comment">//修正构造函数指向</span></span><br><span class="line">B.prototype.cb = <span class="function"><span class="keyword">function</span>(<span class="params"></span>)</span>&#123;</span><br><span class="line">	alert(<span class="built_in">this</span>.b)</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">/* 创建B类的实例 */</span></span><br><span class="line"><span class="keyword">var</span> b = <span class="keyword">new</span> B()</span><br></pre></td></tr></table></figure>

<blockquote>
<p>PS：原型链虽然很强大，用它可以实现 JavaScript 中的继承，但同时也存在着一些问题。原型链继承实际上是在多个构造函数或对象之间共享属性和方法，这种方法在创建子类的对象时，不能向父类的构造函数传递任何参数</p>
</blockquote>
<h3 id="借用构造函数继承"><a href="#借用构造函数继承" class="headerlink" title="借用构造函数继承"></a>借用构造函数继承</h3><p>核心：借用父类构造函数来增强子类实例，等于复制父类实例的属性给子类</p>
<p>例：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">/* 定义A类 */</span></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">A</span>(<span class="params">a</span>)</span>&#123;</span><br><span class="line">    <span class="built_in">this</span>.a = a</span><br><span class="line">    <span class="built_in">this</span>.ca = <span class="function"><span class="keyword">function</span>(<span class="params"></span>)</span>&#123;</span><br><span class="line">        alert(<span class="built_in">this</span>.a)</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">/* 定义B类 */</span></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">B</span>(<span class="params">b</span>)</span>&#123;</span><br><span class="line">    A.call(<span class="built_in">this</span>, <span class="string">&#x27;a&#x27;</span>) <span class="comment">//调用父类构造函数，并绑定 this</span></span><br><span class="line">    <span class="built_in">this</span>.b = b</span><br><span class="line">    <span class="built_in">this</span>.cb = <span class="function"><span class="keyword">function</span>(<span class="params"></span>)</span>&#123;</span><br><span class="line">        alert(<span class="built_in">this</span>.b)</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">/* 创建B类的实例 */</span></span><br><span class="line"><span class="keyword">var</span> b = <span class="keyword">new</span> B(<span class="string">&#x27;b&#x27;</span>)</span><br></pre></td></tr></table></figure>

<blockquote>
<p>PS：借用构造函数继承就是通过 <code>apply()</code> 或 <code>call()</code> 方法在子类构造函数中调用父类的构造函数，并将父类和子类的 this 绑定在一起，这种方式可以向父类传递参数，但不能实现方法的复用</p>
</blockquote>
<h3 id="组合式继承"><a href="#组合式继承" class="headerlink" title="组合式继承"></a>组合式继承</h3><p>核心：通过调用父类构造函数来实现向父类构造函数传递参数，然后通过将父类实例作为子类原型来实现方法复用</p>
<p>例：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">/* 定义A类 */</span></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">A</span>(<span class="params">a</span>)</span>&#123;</span><br><span class="line">    <span class="built_in">this</span>.a = a</span><br><span class="line">&#125;</span><br><span class="line">A.prototype.cs = <span class="function"><span class="keyword">function</span>(<span class="params"></span>)</span>&#123;</span><br><span class="line">    alert(<span class="built_in">this</span>.a)</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">/* 定义B类 */</span></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">B</span>(<span class="params">b</span>)</span>&#123;</span><br><span class="line">    A.call(<span class="built_in">this</span>, <span class="string">&#x27;a&#x27;</span>) <span class="comment">//调用父类构造函数，并绑定 this</span></span><br><span class="line">    <span class="built_in">this</span>.b = b</span><br><span class="line">&#125;</span><br><span class="line">B.prototype = <span class="keyword">new</span> A; <span class="comment">//子类的prototype指向父类的实例对象</span></span><br><span class="line">B.prototype.constructor = B; <span class="comment">//修正构造函数指向</span></span><br><span class="line">B.prototype.cb = <span class="function"><span class="keyword">function</span>(<span class="params"></span>)</span>&#123;</span><br><span class="line">    alert(<span class="built_in">this</span>.b)</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">/* 创建B类的实例 */</span></span><br><span class="line"><span class="keyword">var</span> b = <span class="keyword">new</span> B(<span class="string">&#x27;b&#x27;</span>)</span><br></pre></td></tr></table></figure>

<blockquote>
<p>PS：组合继承是将原型链继承和借用构造函数的技术组合在一起，发挥二者长处的一种继承方式，这样，既通过在原型上定义方法实现了方法的复用，又可以保证每个对象都有自己的专有属性，但这种方法，会调用2次父类的构造函数，因此会存在一份多余的实例属性</p>
</blockquote>
<h3 id="组合寄生式继承"><a href="#组合寄生式继承" class="headerlink" title="组合寄生式继承"></a>组合寄生式继承</h3><p>核心：通过调用父类构造函数来实现向父类构造函数传递参数，然后通过将父类原型作为新对象的原型，并将新对象赋值给子类原型来实现方法复用，并且避免了多余的实例属性</p>
<p>例：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">/* 定义A类 */</span></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">A</span>(<span class="params">a</span>)</span>&#123;</span><br><span class="line">    <span class="built_in">this</span>.a = a</span><br><span class="line">&#125;</span><br><span class="line">A.prototype.cs = <span class="function"><span class="keyword">function</span>(<span class="params"></span>)</span>&#123;</span><br><span class="line">    alert(<span class="built_in">this</span>.a)</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">/* 定义B类 */</span></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">B</span>(<span class="params">b</span>)</span>&#123;</span><br><span class="line">    A.call(<span class="built_in">this</span>, <span class="string">&#x27;a&#x27;</span>) <span class="comment">//调用父类构造函数，并绑定 this</span></span><br><span class="line">    <span class="built_in">this</span>.b = b</span><br><span class="line">&#125;</span><br><span class="line">B.prototype = <span class="built_in">Object</span>.create(A.prototype); <span class="comment">//将父类原型作为新对象的原型，并将新对象赋值给子类原型</span></span><br><span class="line">B.prototype.cb = <span class="function"><span class="keyword">function</span>(<span class="params"></span>)</span>&#123;</span><br><span class="line">    alert(<span class="built_in">this</span>.b)</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">/* 创建B类的实例 */</span></span><br><span class="line"><span class="keyword">var</span> b = <span class="keyword">new</span> B(<span class="string">&#x27;b&#x27;</span>)</span><br></pre></td></tr></table></figure>

<blockquote>
<h3 id="父类和子类"><a href="#父类和子类" class="headerlink" title="父类和子类"></a><span title="基类">父类</span>和子类</h3><p>继承的类称为子类，被继承的类称为父类，子类具有父类的全部属性和方法，但父类不能拥有子类的属性和方法</p>
<h3 id="instanceof-运算符"><a href="#instanceof-运算符" class="headerlink" title="instanceof 运算符"></a>instanceof 运算符</h3><p><strong><code>instanceof</code></strong> <strong>运算符</strong>用于检测构造函数的 <code>prototype</code> 属性是否在某个实例对象的原型链上，如果在，则返回 true，反之返回 false</p>
<p>语法：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">对象 <span class="keyword">instanceof</span> 构造函数</span><br></pre></td></tr></table></figure>

<p>例：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">C</span>(<span class="params"></span>)</span>&#123;&#125;</span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">D</span>(<span class="params"></span>)</span>&#123;&#125;</span><br><span class="line"><span class="keyword">var</span> o = <span class="keyword">new</span> C();</span><br><span class="line">o <span class="keyword">instanceof</span> C; <span class="comment">//返回true</span></span><br><span class="line">o <span class="keyword">instanceof</span> D; <span class="comment">//返回false</span></span><br><span class="line">C.prototype <span class="keyword">instanceof</span> <span class="built_in">Object</span>; <span class="comment">//返回true</span></span><br><span class="line">D.prototype = <span class="keyword">new</span> C(); <span class="comment">//继承</span></span><br><span class="line"><span class="keyword">var</span> o2 = <span class="keyword">new</span> D();</span><br><span class="line">o2 <span class="keyword">instanceof</span> D; <span class="comment">//返回true</span></span><br><span class="line">o2 <span class="keyword">instanceof</span> C; <span class="comment">//返回true</span></span><br></pre></td></tr></table></figure>
</blockquote>
]]></content>
      <categories>
        <category>Web开发</category>
      </categories>
      <tags>
        <tag>JavaScript</tag>
      </tags>
  </entry>
  <entry>
    <title>JavaScript(一) 发展与使用</title>
    <url>/posts/72fcc0e1/</url>
    <content><![CDATA[<link rel="stylesheet" class="aplayer-secondary-style-marker" href="/assets/css/APlayer.min.css"><script src="/assets/js/APlayer.min.js" class="aplayer-secondary-script-marker"></script><p>JavaScript 是一种具有<strong>函数优先</strong>特性的轻量级、<strong>解释型</strong>( 无需手动编译，运行时边解释边执行 )的编程语言，它是运行在 Web浏览器中的一种脚本语言，当然，也还可被运行在一些非浏览器环境中，如：Node.js</p>
<p>进一步说，JavaScript 是一种基于原型、多范式、单线程的<strong>动态语言</strong>( 无需指定类型，运行时确定类型 )，并且支持面向对象、命令式和声明式( 如函数式编程 )风格</p>
<h2 id="历史"><a href="#历史" class="headerlink" title="历史"></a>历史</h2><blockquote>
<ul>
<li>JavaScript 最初由 Netscape( 网景公司 )的一位工程师，名叫( <a href="https://baike.baidu.com/item/Brendan%20Eich/561441">Brendan Eich</a> )设计的，起初命名为 LiveScript，后来由于 Netscape公司在与 Sun公司合作之后将其改名为 JavaScript</li>
</ul>
</blockquote>
<blockquote>
<ul>
<li>JavaScript 最初受 Java启发而开始设计的，目的之一就是“看上去像Java”，因此语法上有类似之处，一些名称和命名规范也借自 Java。但 JavaScript 的主要设计原则源自 Self 和 Scheme。JavaScript 与 Java名称上的近似，是当时 Netscape 为了营销考虑与 Sun微系统达成协议的结果。为了取得技术优势，微软推出了 JScript 来迎战 JavaScript 的脚本语言</li>
</ul>
</blockquote>
<p><img src="https://cdn3.tianli0.top/gh/k34869/images/image/20221206161239" alt="三大公司"></p>
<blockquote>
<ul>
<li>后来，为了互用性，Netscape 公司将 JavaScript 提交给欧洲计算机制造商协会( ECMA )，制定了 ECMA-262标准（ECMAScript）</li>
</ul>
</blockquote>
<blockquote>
<ul>
<li>发展初期，JavaScript 的标准并未确定，同期有 Netscape 的 JavaScript，微软的 JScript 和 CEnvi 的 ScriptEase 三足鼎立。1997年，在ECMA（欧洲计算机制造商协会）的协调下，由 Netscape、Sun、微软、Borland 组成的工作组确定统一标准：ECMA-262</li>
</ul>
</blockquote>
<h3 id="时间表"><a href="#时间表" class="headerlink" title="时间表"></a>时间表</h3><table>
<thead>
<tr>
<th>年份</th>
<th>事件</th>
</tr>
</thead>
<tbody><tr>
<td>1995</td>
<td>网景公司开发了 JavaScript 语言</td>
</tr>
<tr>
<td>1996</td>
<td>微软发布了和 JavaScript 兼容的 JScript</td>
</tr>
<tr>
<td>1997</td>
<td>ECMAScript 1.0 版（ECMA-262）发布</td>
</tr>
<tr>
<td>1998</td>
<td>ECMAScript 2.0 版 发布</td>
</tr>
<tr>
<td>1998</td>
<td>DOM Level1 的制定</td>
</tr>
<tr>
<td>1998</td>
<td>新技术 DHTML 登场</td>
</tr>
<tr>
<td>1999</td>
<td>ECMAScript 3.0 版 发布</td>
</tr>
<tr>
<td>2000</td>
<td>DOM Level2 的制定</td>
</tr>
<tr>
<td>2002</td>
<td>ISO/IEC 16262:2002 的确立</td>
</tr>
<tr>
<td>2004</td>
<td>DOM Level3 的制定</td>
</tr>
<tr>
<td>2005</td>
<td>新技术 AJAX 登场</td>
</tr>
<tr>
<td>2009</td>
<td>ECMAScript 5.0 版 发布</td>
</tr>
<tr>
<td>2009</td>
<td>新技术 HTML5 登场</td>
</tr>
<tr>
<td>2011</td>
<td>ISO/IEC 16262:2011 的确立</td>
</tr>
<tr>
<td>2013</td>
<td>ECMAScript 6.0 版 发布</td>
</tr>
</tbody></table>
<h2 id="实现"><a href="#实现" class="headerlink" title="实现"></a>实现</h2><blockquote>
<p>ECMAScript 是一个标准而这个标准需要由各个浏览器厂商去实现</p>
<p>不同浏览器厂商对该标准有不同的实现</p>
</blockquote>
<table>
<thead>
<tr>
<th>浏览器</th>
<th>JavaScript 引擎( 实现 )</th>
</tr>
</thead>
<tbody><tr>
<td>FireFox</td>
<td>SpiderMonkey</td>
</tr>
<tr>
<td>Internet Explorer</td>
<td>JScript/Chakra</td>
</tr>
<tr>
<td>Safari</td>
<td>JavaScriptCore</td>
</tr>
<tr>
<td>Chrome</td>
<td>V8</td>
</tr>
<tr>
<td>Opera</td>
<td>Carakan</td>
</tr>
</tbody></table>
<h2 id="组成"><a href="#组成" class="headerlink" title="组成"></a>组成</h2><p>ECMAScript 是 JavaScript的标准，所以一般情况下这两个词我们认为是一个意思，但实际上 JavaScript 的含义却要更大一些。一个完整的浏览器 JavaScript 实现由以下三部分组成：</p>
<ul>
<li>ECMAScript（描述了该语言的语法和基本对象）</li>
<li>文档对象模型（DOM）描述处理网页内容的方法和接口</li>
<li>浏览器对象模型（BOM）描述与浏览器进行交互的方法和接口</li>
</ul>
<h2 id="代码编写位置"><a href="#代码编写位置" class="headerlink" title="代码编写位置"></a>代码编写位置</h2><h3 id="1、内部JS代码"><a href="#1、内部JS代码" class="headerlink" title="1、内部JS代码"></a>1、内部JS代码</h3><p>直接将 js 代码写在<code>&lt;script&gt;&lt;/script&gt;</code>标签内，浏览器检索到<code>&lt;script&gt;</code>标签时会自动调用 <strong>JavaScript引擎</strong> 来执行 js 代码</p>
<p>例：</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">script</span> <span class="attr">type</span>=<span class="string">&quot;text/javascript&quot;</span>&gt;</span><span class="javascript"></span></span><br><span class="line"><span class="javascript">  	<span class="comment">//第一个Js程序</span></span></span><br><span class="line"><span class="javascript">    <span class="built_in">console</span>.log(<span class="string">&quot;Hello World&quot;</span>);</span></span><br><span class="line"><span class="javascript"></span><span class="tag">&lt;/<span class="name">script</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p><strong>属性：</strong></p>
<ul>
<li><code>type</code>：默认值<code>text/javascript</code>可省略，默认还是这个值</li>
</ul>
<h3 id="2、外部JS代码"><a href="#2、外部JS代码" class="headerlink" title="2、外部JS代码"></a>2、外部JS代码</h3><p>例：</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="comment">&lt;!-- hello.html --&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">script</span> <span class="attr">src</span>=<span class="string">&quot;./hello.js&quot;</span>&gt;</span><span class="tag">&lt;/<span class="name">script</span>&gt;</span></span><br><span class="line"><span class="comment">&lt;!-- 通过&lt;script&gt;标签的src属性来引入外部js文件 --&gt;</span></span><br></pre></td></tr></table></figure>

<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">/*</span></span><br><span class="line"><span class="comment"> hello.js</span></span><br><span class="line"><span class="comment">*/</span></span><br><span class="line"><span class="built_in">console</span>.log(<span class="string">&quot;Hello World&quot;</span>);</span><br><span class="line"><span class="comment">//在外部js文件编写js代码</span></span><br></pre></td></tr></table></figure>

<blockquote>
<ul>
<li><code>//..</code> 行注释</li>
<li><code>/* ..注释内容.. */</code> 块注释</li>
<li>注释的内容则会被 <strong>js引擎</strong> 忽略不执行</li>
</ul>
</blockquote>
]]></content>
      <categories>
        <category>Web开发</category>
      </categories>
      <tags>
        <tag>JavaScript</tag>
      </tags>
  </entry>
  <entry>
    <title>JavaScript(三) 运算符与表达式</title>
    <url>/posts/ca7331ed/</url>
    <content><![CDATA[<link rel="stylesheet" class="aplayer-secondary-style-marker" href="/assets/css/APlayer.min.css"><script src="/assets/js/APlayer.min.js" class="aplayer-secondary-script-marker"></script><p>运算符又称为操作符，运算符就是用于对数据进行各种运算的符号，JavaScript 有算术运算符、赋值运算符、关系运算符等</p>
<h2 id="算数运算符"><a href="#算数运算符" class="headerlink" title="算数运算符"></a>算数运算符</h2><p>算数运算符包括 加(<code>+</code>) 减(<code>-</code>) 乘(<code>*</code>) 除(<code>/</code>)和 取模(<code>%</code>) 运算符，对于非Number类型的数据值进行算数运算时，会将这些值转换为Number类型然会在运算，其中任何数据类型的值和 NaN 做运算都得 NaN</p>
<ul>
<li><code>+</code>可以对两个值进行加法运算，并将结果返回</li>
<li><code>- </code>可以对两个值进行减法运算，并将结果返回</li>
<li><code>*</code>可以对两个值进行乘法运算，并将结果返回</li>
<li><code>/ </code>可以对两个值进行除法运算，并将结果返回</li>
<li><code>%</code>可以对两个值进行取模运算（取余数），并将结果返回</li>
</ul>
<p>例：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="built_in">console</span>.log(<span class="number">100</span>+<span class="number">100</span>); <span class="comment">//输出：200</span></span><br><span class="line"><span class="built_in">console</span>.log(<span class="number">2</span>-<span class="literal">true</span>); <span class="comment">//输出：1</span></span><br><span class="line"><span class="built_in">console</span>.log(<span class="literal">undefined</span>*<span class="literal">NaN</span>); <span class="comment">//输出：NaN</span></span><br></pre></td></tr></table></figure>

<h3 id="字符串加法运算"><a href="#字符串加法运算" class="headerlink" title="字符串加法运算"></a>字符串加法运算</h3><p>运算关系：</p>
<ul>
<li>任何数据类型的值与字符串相加时会先将其转换为字符串，然后将两个字符串拼接成一个字符串( 拼串 )</li>
<li>我们可以让任意的数据类型<code>+&quot;&quot;</code>( 空串 )使其转换为字符串( 隐式转换 )</li>
</ul>
<p>例：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="built_in">console</span>.log(<span class="number">123</span> + <span class="string">&quot;1&quot;</span>); <span class="comment">//输出：&quot;1231&quot;</span></span><br><span class="line"><span class="built_in">console</span>.log(<span class="string">&quot;年龄：&quot;</span> + <span class="string">&quot;20&quot;</span>); <span class="comment">//输出：&quot;年龄：20&quot;</span></span><br><span class="line"><span class="built_in">console</span>.log(<span class="literal">NaN</span> + <span class="string">&quot; 20&quot;</span>); <span class="comment">//输出：&quot;NaN 20&quot;</span></span><br><span class="line"><span class="built_in">console</span>.log(<span class="number">200</span> + <span class="string">&quot;&quot;</span>); <span class="comment">//输出：&quot;200&quot;</span></span><br></pre></td></tr></table></figure>

<ul>
<li>加号还可以用来连接下一行的表达式</li>
</ul>
<p>例：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="built_in">console</span>.log(<span class="string">&quot;锄禾日当午，&quot;</span> +</span><br><span class="line">		  <span class="string">&quot;汗滴禾下土。&quot;</span> +</span><br><span class="line">		  <span class="string">&quot;谁吃盘中餐，&quot;</span> +</span><br><span class="line">		  <span class="string">&quot;粒粒皆辛苦。&quot;</span>);    <span class="comment">//输出：&quot;锄禾日当午，汗滴禾下土。谁吃盘中餐，粒粒皆辛苦。&quot;</span></span><br></pre></td></tr></table></figure>

<h2 id="赋值运算符"><a href="#赋值运算符" class="headerlink" title="赋值运算符 ( = )"></a>赋值运算符 ( = )</h2><p>可以将等号右边的值赋值给等号左边的变量中，但要注意变量一定要先声明，在赋值</p>
<p><strong>语法：</strong> <code>变量名 = 5 * 10;</code></p>
<h3 id="复合运算："><a href="#复合运算：" class="headerlink" title="复合运算："></a>复合运算：</h3><p><code>a+=5</code> 等同于 <code>a = a + 5</code></p>
<p><code>a-=5</code> 等同于 <code>a = a - 5</code></p>
<p><code>a*=5 </code> 等同于 <code>a = a * 5</code></p>
<p><code>a/=5 </code> 等同于 <code>a = a / 5</code></p>
<p><code>a%=5 </code> 等同于 <code>a = a % 5</code></p>
<h2 id="一元运算符"><a href="#一元运算符" class="headerlink" title="一元运算符"></a>一元运算符</h2><p>上面的运算符中都有两个操作数，所以它们也叫二元运算符，如果只有一个操作数那它就是一元运算符</p>
<p>带 <code>+</code> 号的数即正数，带 <code>-</code> 号的数即负数，正号(<code>+</code>)不会对数字产生任何影响，如果在非Number数据类型的值前加上正号(<code>+</code>)即可将其转换为Number类型( 隐式转换 )</p>
<p>例：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> a = <span class="literal">true</span>;</span><br><span class="line">a = +a;</span><br><span class="line"><span class="built_in">console</span>.log(a); <span class="comment">//输出：1</span></span><br></pre></td></tr></table></figure>

<h3 id="自增与自减运算符"><a href="#自增与自减运算符" class="headerlink" title="自增与自减运算符"></a>自增与自减运算符</h3><p>自增运算符( ++ )，自减运算符( – )，自增和自减运算符会使**<span title="仅对变量有效">变量</span>**的值自动 +1 或 -1</p>
<p>自增和自减分为两种，分别是前置自增和后置自增、前置自减和后置自减</p>
<p><strong>语法</strong>：<code>++变量</code> or <code>变量++</code> / <code>--变量</code> or <code>变量--</code></p>
<blockquote>
<p>PS：其中后置自减和后置自增返回的是自增和自减前的值，而前置自减和前置自增返回的是自增和自减后的值</p>
</blockquote>
<p>例：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> a = <span class="number">5</span></span><br><span class="line"><span class="built_in">console</span>.log(a, a++, a, ++a, a) <span class="comment">//输出：5 5 6 7 7</span></span><br><span class="line"><span class="built_in">console</span>.log(a, a--, a, --a, a) <span class="comment">//输出：7 7 6 5 5</span></span><br></pre></td></tr></table></figure>

<h2 id="逻辑运算符"><a href="#逻辑运算符" class="headerlink" title="逻辑运算符"></a>逻辑运算符</h2><h3 id="与运算符-amp-amp"><a href="#与运算符-amp-amp" class="headerlink" title="与运算符 ( &amp;&amp; )"></a>与运算符 ( &amp;&amp; )</h3><p>语法：<code>表达式 &amp;&amp; 表达式</code></p>
<ul>
<li>运算时会将符号左边的值转换成布尔型再运算</li>
<li>如果符号左边的表达式返回值为<code>true</code>，则结果为符号右边表达式的值，反之，如果符号左边的表达式返回值为<code>false</code>，则结果为符号左边表达式的值</li>
</ul>
<p>例：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> a, b;</span><br><span class="line">a = <span class="literal">NaN</span> &amp;&amp; <span class="number">5</span>;</span><br><span class="line"><span class="built_in">console</span>.log(a); <span class="comment">//输出：false</span></span><br><span class="line"><span class="built_in">console</span>.log(b = <span class="string">&quot;你好&quot;</span> &amp;&amp; ++a); <span class="comment">//输出：1</span></span><br></pre></td></tr></table></figure>

<h3 id="或运算符"><a href="#或运算符" class="headerlink" title="或运算符 ( || )"></a>或运算符 ( || )</h3><p>语法：<code>表达式 || 表达式</code></p>
<ul>
<li><p>运算时会将符号左边的值转换成布尔型再运算</p>
</li>
<li><p>如果符号左边的表达式返回值为<code>false</code>，则结果为符号右边表达式的值，反之，如果符号左边的表达式返回值为<code>true</code>，则结果为符号左边表达式的值</p>
</li>
</ul>
<p>例：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> a = <span class="number">55</span>-<span class="number">5</span> || <span class="number">5</span>;</span><br><span class="line"><span class="built_in">console</span>.log(a); <span class="comment">//输出：50</span></span><br><span class="line"><span class="built_in">console</span>.log(b = <span class="literal">false</span> || ++a); <span class="comment">//输出：51</span></span><br></pre></td></tr></table></figure>

<h3 id="非运算符"><a href="#非运算符" class="headerlink" title="非运算符 ( ! )"></a>非运算符 ( ! )</h3><p>语法：<code>!表达式</code></p>
<ul>
<li>对布尔类型数据进行反向运算( 取反 )</li>
<li><code>!</code> 运算符只有一个操作数，所以属于一元运算符</li>
<li>非布尔类型数据取反会先将其转换为布尔型在运算</li>
</ul>
<p>例：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> a = <span class="literal">true</span>;</span><br><span class="line">a = !a;</span><br><span class="line"><span class="built_in">console</span>.log(a); <span class="comment">//输出false</span></span><br><span class="line"><span class="comment">//还可以多次取反 !!a</span></span><br></pre></td></tr></table></figure>

<h2 id="关系运算符"><a href="#关系运算符" class="headerlink" title="关系运算符"></a>关系运算符</h2><p>通过关系运算符可以比较两个值关系，如果关系成立 则返回 true，如果不成立 则返回 false</p>
<p>关系型运算符包括：</p>
<ul>
<li><p><code>&gt;</code>(大于)、<code>&gt;=</code>(大于等于)、<code>!&gt;</code>(不大于)</p>
</li>
<li><p><code>&lt;</code>(小于)、<code>&lt;=</code>(小于等于)、<code>!&lt;</code>(不小于)</p>
</li>
</ul>
<p>以上的关系运算符在运算时会将符号左右的操作数转换成数值型再做运算，其中NaN不和任何值相等，包括它本身，所以任何数据类型的值在和NaN做关系运算时都会返回 false</p>
<p>例：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="built_in">console</span>.log(<span class="number">1</span> &gt; <span class="literal">true</span>) <span class="comment">//输出：false</span></span><br><span class="line"><span class="built_in">console</span>.log(<span class="literal">true</span> == <span class="string">&quot;1&quot;</span>); <span class="comment">//输出：true</span></span><br><span class="line"><span class="built_in">console</span>.log(<span class="number">1</span> &gt;= <span class="literal">true</span>) <span class="comment">//输出：true</span></span><br><span class="line"><span class="built_in">console</span>.log(<span class="number">1</span> &gt; <span class="string">&#x27;0&#x27;</span>) <span class="comment">//输出：true</span></span><br><span class="line"><span class="built_in">console</span>.log(<span class="number">10</span> &gt; <span class="literal">null</span>) <span class="comment">//输出：true</span></span><br><span class="line"><span class="built_in">console</span>.log(<span class="number">10</span> &lt;= <span class="string">&quot;hello&quot;</span>) <span class="comment">//输出：false</span></span><br><span class="line"><span class="built_in">console</span>.log(<span class="number">1000000000</span> &gt; <span class="literal">NaN</span>) <span class="comment">//输出：false</span></span><br><span class="line"><span class="built_in">console</span>.log(<span class="literal">true</span> &gt; <span class="literal">false</span>) <span class="comment">//输出：true</span></span><br></pre></td></tr></table></figure>

<blockquote>
<p>PS：null 在做相等运算(<code>==</code>)时不会自动转换为数值型，由于 undefined 衍生自 null，所以这两个值做相等运算时，会返回 true</p>
<p>例：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="built_in">console</span>.log(<span class="literal">undefined</span> == <span class="literal">null</span>) <span class="comment">//输出：true</span></span><br></pre></td></tr></table></figure>
</blockquote>
<p>Js中还有两个关系运算符，分别为：</p>
<ul>
<li><code>===</code>(全等)</li>
<li><code>!==</code>(不全等)</li>
</ul>
<p>全等和不全等运算符在做运算时，不会做自动数据类型转换，如果两个不同数据类型的值做全等(<code>===</code>)运算时，直接返回 false，同样的，如果两个不同数据类型的值做不全等(<code>!==</code>)运算时，直接返回 true</p>
<p>例：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="built_in">console</span>.log(<span class="number">1</span> === <span class="string">&quot;1&quot;</span>) <span class="comment">//输出：false</span></span><br><span class="line"><span class="built_in">console</span>.log(<span class="number">1</span> !== <span class="number">1</span>) <span class="comment">//输出：false</span></span><br><span class="line"><span class="built_in">console</span>.log(<span class="number">1</span> !== <span class="string">&quot;1&quot;</span>) <span class="comment">//输出：true</span></span><br></pre></td></tr></table></figure>

<h3 id="isNaN"><a href="#isNaN" class="headerlink" title="isNaN()"></a>isNaN()</h3><p>通过<code>isNaN()</code>函数可以判断一个值是否为NaN，如果为 <code>NaN</code>则返回值为<code>true</code>，否则为<code>false</code>。</p>
<p>例：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> a = <span class="string">&#x27;abc&#x27;</span></span><br><span class="line">a = <span class="built_in">Number</span>(a)</span><br><span class="line"><span class="built_in">console</span>.log(a, <span class="built_in">isNaN</span>(a)) <span class="comment">//输出：NaN, true</span></span><br><span class="line"><span class="built_in">console</span>.log(<span class="built_in">isNaN</span>(<span class="literal">NaN</span>)) <span class="comment">//输出：true</span></span><br></pre></td></tr></table></figure>


<h2 id="条件运算符"><a href="#条件运算符" class="headerlink" title="条件运算符"></a>条件运算符</h2><p>条件运算符有3个操作数，所以也称为 <strong>三元运算符</strong></p>
<p>语法：<code>条件 ? 表达式1 : 表达式2</code></p>
<ul>
<li><p>如果条件值为 true(非布尔值自动转换) 则执行表达式1，并返回其值，反之亦然.</p>
</li>
<li><p>三元运算符是对if……else语句的一种简化写法</p>
</li>
</ul>
<p>下面的范例中，如果变量 age 中的值小于 18，则向变量 voteable 赋值 “年龄太小”，否则赋值 “年龄已达到”</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">voteable = (age &lt; <span class="number">18</span>) ? <span class="string">&quot;年龄太小&quot;</span> : <span class="string">&quot;年龄已达到&quot;</span>;</span><br></pre></td></tr></table></figure>

<p>案例：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 是否年满18岁</span></span><br><span class="line"><span class="keyword">var</span> age = <span class="built_in">parseInt</span>(prompt(<span class="string">&quot;请您输入年龄&quot;</span>));</span><br><span class="line"><span class="keyword">var</span> s = (age &gt;= <span class="number">18</span>) ? <span class="string">&#x27;18岁了&#x27;</span> : <span class="string">&#x27;未满18岁&#x27;</span>;</span><br><span class="line"><span class="built_in">console</span>.log(s);</span><br></pre></td></tr></table></figure>

<h2 id="表达式"><a href="#表达式" class="headerlink" title="表达式"></a>表达式</h2><p>表达式是指可以进行运算，且必须返回一个确定的值的式子，一般由常量、变量、运算符、子表达式构成，最简单的表达式可以是一个简单的值( 常量 or 变量 )</p>
<p>例：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">/* 值表达式 */</span></span><br><span class="line"><span class="number">1</span>			<span class="comment">//数字表达式</span></span><br><span class="line"><span class="string">&#x27;a&#x27;</span>		<span class="comment">//字符串表达式</span></span><br><span class="line"><span class="literal">true</span>	<span class="comment">//布尔值表达式</span></span><br><span class="line">a			<span class="comment">//变量表达式</span></span><br></pre></td></tr></table></figure>

<p>在实际编程中，可以使用运算符建立复杂的表达式</p>
<p>例：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="number">4</span> + <span class="number">3</span> - <span class="number">2</span></span><br><span class="line">a = b * <span class="number">3</span> <span class="comment">// 赋值表达式 返回赋值结果</span></span><br><span class="line">a + <span class="number">3</span> &gt; b + <span class="number">3</span></span><br></pre></td></tr></table></figure>

<p>运算符一般由一些符号组成，如 “+” “/” “=” “%” 等，也有些运算符使用关键字表示，如 typeof</p>
<blockquote>
<h3 id="词（token）"><a href="#词（token）" class="headerlink" title="词（token）"></a>词（token）</h3><p>词是 JavaScript 中的最小语义单元，包括常量、变量、运算符、表达式、指令等</p>
<p>例：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">/* 变量声明语句 */</span></span><br><span class="line"><span class="keyword">var</span> a = <span class="number">123</span>;</span><br><span class="line"><span class="comment">/* 组成 */</span></span><br><span class="line"><span class="keyword">var</span>	<span class="comment">//指令(语句标识符)</span></span><br><span class="line">a		<span class="comment">//变量名(标识符)</span></span><br><span class="line">=		<span class="comment">//赋值运算符</span></span><br><span class="line"><span class="number">123</span>	<span class="comment">//值(数字)</span></span><br><span class="line">;		<span class="comment">//运算符</span></span><br></pre></td></tr></table></figure>

<h3 id="语句（statement）"><a href="#语句（statement）" class="headerlink" title="语句（statement）"></a>语句（statement）</h3><p>在高级语言中，语句是一个能够对计算机下达<strong>指令</strong>的程序代码</p>
<p>JavaScript 语句通常以一个语句标识符为开始，并执行该语句。语句标识符用来标识程序要执行的动作，语句标识符为保留关键字不能作为任何自定义的标识符使用</p>
<p>多条语句之间用分号( <code>;</code> )分隔</p>
</blockquote>
]]></content>
      <categories>
        <category>Web开发</category>
      </categories>
      <tags>
        <tag>JavaScript</tag>
      </tags>
  </entry>
  <entry>
    <title>JavaScript(二) 变量与数据类型</title>
    <url>/posts/9babe0a3/</url>
    <content><![CDATA[<link rel="stylesheet" class="aplayer-secondary-style-marker" href="/assets/css/APlayer.min.css"><script src="/assets/js/APlayer.min.js" class="aplayer-secondary-script-marker"></script><h2 id="字面量"><a href="#字面量" class="headerlink" title="字面量"></a>字面量</h2><p>字面量(literal)又称直接量，是指程序中能够<strong>直接使用</strong>的值，如 3.1415926</p>
<p><strong>数字（Number）字面量</strong> 可以是整数或者是小数，或者是科学计数(e)</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="number">3.14</span></span><br><span class="line"><span class="number">1001</span></span><br><span class="line"><span class="number">123e5</span></span><br></pre></td></tr></table></figure>

<p><strong>字符串（String）字面量</strong> 可以使用单引号或双引号</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="string">&quot;John Doe&quot;</span></span><br><span class="line"><span class="string">&#x27;John Doe&#x27;</span></span><br></pre></td></tr></table></figure>

<p><strong>表达式字面量</strong> 用于计算</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="number">5</span> + <span class="number">6</span></span><br><span class="line"><span class="number">5</span> * <span class="number">10</span></span><br></pre></td></tr></table></figure>

<p><strong>数组（Array）字面量</strong> 用于定义一个数组</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">[<span class="number">40</span>, <span class="number">100</span>, <span class="number">1</span>, <span class="number">5</span>, <span class="number">25</span>, <span class="number">10</span>]</span><br></pre></td></tr></table></figure>

<p><strong>对象（Object）字面量</strong> 用于定义一个对象</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">&#123;<span class="attr">firstName</span>:<span class="string">&quot;John&quot;</span>, <span class="attr">lastName</span>:<span class="string">&quot;Doe&quot;</span>, <span class="attr">age</span>:<span class="number">50</span>, <span class="attr">eyeColor</span>:<span class="string">&quot;blue&quot;</span>&#125;</span><br></pre></td></tr></table></figure>

<h2 id="变量（variable）"><a href="#变量（variable）" class="headerlink" title="变量（variable）"></a>变量（variable）</h2><p>变量是程序中数据的临时存放场所。在代码中可以只使用一个变量，也可以使用多个变量</p>
<h3 id="声明变量"><a href="#声明变量" class="headerlink" title="声明变量"></a>声明变量</h3><p>JavaScript 使用 <strong>var</strong> 关键字来声明变量</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> carname; <span class="comment">// 变量声明</span></span><br></pre></td></tr></table></figure>

<p>变量声明之后，该变量默认值为<code>undefined</code></p>
<p>如需向变量赋值，请使用等号( <code>=</code> )</p>
<p><code>carname=&quot;Volvo&quot;;</code></p>
<p>我们也可以在声明变量时对其赋值</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> carname = <span class="string">&quot;Volvo&quot;</span>; <span class="comment">// 定义一个变量</span></span><br></pre></td></tr></table></figure>

<p>下面的代码定义了一个名为 <strong>carname</strong> 的变量，并向其赋值 <code>&quot;Volvo&quot;</code>，然后把它放入 <code>id=&quot;demo&quot;</code> 的 HTML 段落中</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">p</span> <span class="attr">id</span>=<span class="string">&quot;demo&quot;</span>&gt;</span><span class="tag">&lt;/<span class="name">p</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">script</span>&gt;</span><span class="javascript"></span></span><br><span class="line"><span class="javascript"><span class="keyword">var</span> carname = <span class="string">&quot;Volvo&quot;</span>;</span></span><br><span class="line"><span class="javascript"><span class="built_in">document</span>.getElementById(<span class="string">&quot;demo&quot;</span>).innerHTML = carname;</span></span><br><span class="line"><span class="javascript"></span><span class="tag">&lt;/<span class="name">script</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>如果一个访问一个不存在( 未声明 + 未定义 )的变量就会提示以下错误：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="built_in">console</span>.log(message);</span><br><span class="line"><span class="comment">//提示错误：message is not defined</span></span><br></pre></td></tr></table></figure>

<blockquote>
<p><strong>默认情况下</strong> 不写声明关键字 <code>var</code> 直接赋值也可以使用变量，例如：<code>a = 123</code></p>
</blockquote>
<h3 id="一条语句，多个变量"><a href="#一条语句，多个变量" class="headerlink" title="一条语句，多个变量"></a>一条语句，多个变量</h3><p>我们可以在一条语句中声明很多变量</p>
<p>该语句以 var 开头，并使用逗号 ( , ) 分隔变量即可</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> lastname=<span class="string">&quot;Doe&quot;</span>, age=<span class="number">30</span>, job=<span class="string">&quot;carpenter&quot;</span>;</span><br></pre></td></tr></table></figure>

<h2 id="标识符（identifier）"><a href="#标识符（identifier）" class="headerlink" title="标识符（identifier）"></a>标识符（identifier）</h2><p>所谓标识符，就是指变量、函数、属性的名字，或函数的参数</p>
<p>JavaScript 使用 Unicode 字符集，理论上标识符可以是任何字符( 包括中文 )，但考虑到兼容性和安全性问题，建议遵循规范命名</p>
<p>标识符需要按照以下规范命名：</p>
<ul>
<li>标识符可以是字母(A-Z.a-z)、数字(0-9)、下划线 _ 和 $，但第一个字符不能为数字(0-9)</li>
<li>标识符还不能以 JavaScript <strong>保留关键字</strong> 命名</li>
</ul>
<p><img src="https://cdn1.tianli0.top/gh/k34869/images/image/20221112102250" alt="keywords"></p>
<blockquote>
<h3 id="字母大小写"><a href="#字母大小写" class="headerlink" title="字母大小写"></a>字母大小写</h3><p>JavaScript 是区分字母大小写的</p>
<p>函数 <strong>getElementById</strong> 与 <strong>getElementbyID</strong> 是不同的</p>
<p>同样，变量 <strong>myVariable</strong> 与 <strong>MyVariable</strong> 也是不同的</p>
<p>JavaScript 中，常见的是驼峰法的命名规则，如 lastName (而不是lastname)</p>
</blockquote>
<h2 id="数据类型（dataType）"><a href="#数据类型（dataType）" class="headerlink" title="数据类型（dataType）"></a>数据类型（dataType）</h2><p>JavaScript 中有 5 种基本的数据类型和 1 种引用数据类型：</p>
<p>基本数据类型( Primitive Data Type )：</p>
<ol>
<li>字符串 ( <strong>String</strong> )</li>
<li>数字 ( <strong>Number</strong> )</li>
<li>布尔值 ( <strong>Boolean</strong> )</li>
<li>空 ( <strong>null</strong> )</li>
<li>未定义 ( <strong>undefined</strong> )</li>
</ol>
<p>引用数据类型( Reference Data Type )：</p>
<ul>
<li>对象( <strong>Object</strong> )</li>
</ul>
<h3 id="动态数据类型"><a href="#动态数据类型" class="headerlink" title="动态数据类型"></a>动态数据类型</h3><p>JavaScript 声明变量是无需指定数据类型的，js 运行时会根据变量的值确定数据类型，这意味着相同的变量可存储不同的数据类型</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> x;      <span class="comment">// x 为 undefined </span></span><br><span class="line">x = <span class="number">5</span>;      <span class="comment">// 现在 x 为数字</span></span><br><span class="line">x = <span class="string">&quot;John&quot;</span>; <span class="comment">// 现在 x 为字符串</span></span><br></pre></td></tr></table></figure>

<h2 id="字符串（String）"><a href="#字符串（String）" class="headerlink" title="字符串（String）"></a>字符串（String）</h2><p>多个字符连在一起就叫做字符串，字符串用引号包裹，引号可以是单引号也可以是双引号</p>
<p>字符串可以是引号中的任意文本</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> carname = <span class="string">&quot;Volvo XC60&quot;</span>;</span><br><span class="line"><span class="keyword">var</span> carname = <span class="string">&#x27;Volvo XC60&#x27;</span>;</span><br></pre></td></tr></table></figure>

<p>字符串中可以包含引号，只要不和包围字符串的引号相同就行</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> answer=<span class="string">&quot;It&#x27;s alright&quot;</span>;</span><br><span class="line"><span class="keyword">var</span> answer=<span class="string">&quot;He is called &#x27;Johnny&#x27;&quot;</span>;</span><br><span class="line"><span class="keyword">var</span> answer=<span class="string">&#x27;He is called &quot;Johnny&quot;&#x27;</span>;</span><br><span class="line"><span class="keyword">var</span> answer=<span class="string">&quot;He is called &quot;</span>Johnny<span class="string">&quot;&quot;</span>; <span class="comment">// 错误 Johnny会被当做变量</span></span><br></pre></td></tr></table></figure>

<h3 id="转义字符"><a href="#转义字符" class="headerlink" title="转义字符"></a>转义字符</h3><p>字符串中一些特殊符号是无法直接使用的，这时就需要使用转义字符了</p>
<table>
<caption>
JavaScript 转义字符</caption>
<tbody>
<tr>
<th>
序列</th>
<th>
代表字符</th>
</tr>
<tr>
<td>
\0</td>
<td>
Null字符（\u0000）</td>
</tr>
<tr>
<td>
\b</td>
<td>
退格符（\u0008）</td>
</tr>
<tr>
<td>
\t</td>
<td>
水平制表符（\u0009）</td>
</tr>
<tr>
<td>
\n</td>
<td>
换行符（\u000A）</td>
</tr>
<tr>
<td>
\v</td>
<td>
垂直制表符（\u000B）</td>
</tr>
<tr>
<td>
\f</td>
<td>
换页符（\u000C）</td>
</tr>
<tr>
<td>
\r</td>
<td>
回车符（\u000D）</td>
</tr>
<tr>
<td>
\"</td>
<td>
双引号（\u0022）</td>
</tr>
<tr>
<td>
\'</td>
<td>
撇号或单引号（\u0027）</td>
</tr>
<tr>
<td>
\\</td>
<td>
反斜杠（\u005C）</td>
</tr>
<tr>
<td>
\uXXXX</td>
<td>
由 4 位十六进制数值 XXXX 指定的 Unicode 字符 <a href="https://www.unicode.org/Public/13.0.0/charts/CodeCharts.pdf" title="CodeCharts">Unicode编码表参考书</a></td>
</tr>
</tbody>
</table>


<h2 id="数字（Number）"><a href="#数字（Number）" class="headerlink" title="数字（Number）"></a>数字（Number）</h2><p>数字 主要用于算数运算</p>
<p>数字可以是整数还可以是小数</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> a = <span class="number">13</span>;</span><br><span class="line"><span class="keyword">var</span> b = <span class="number">13.00</span>;</span><br></pre></td></tr></table></figure>

<p>对于极大或极小的数字可以通过科学 ( 指数 ) 计数法来表示</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> y=<span class="number">123e5</span>;      <span class="comment">// 12300000</span></span><br><span class="line"><span class="keyword">var</span> z=<span class="number">123e-5</span>;     <span class="comment">// 0.00123</span></span><br></pre></td></tr></table></figure>

<h3 id="±infinity"><a href="#±infinity" class="headerlink" title="±infinity"></a>±infinity</h3><p>Number型的数字大小是有限的，通过输出常量<code>Number.MAX_VALUE</code>和<code>Number.MIN_VALUE</code>即可获取 js 的最大数和最小数</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="built_in">console</span>.log(<span class="built_in">Number</span>.MAX_VALUE);</span><br><span class="line"><span class="comment">// Number.MAX_VALUE 为Js所能表示的最大正数</span></span><br><span class="line"><span class="built_in">console</span>.log(-<span class="built_in">Number</span>.MAX_VALUE);</span><br><span class="line"><span class="comment">// -Number.MAX_VALUE 为Js所能表示的最大负数</span></span><br><span class="line"><span class="built_in">console</span>.log(<span class="built_in">Number</span>.MIN_VALUE);</span><br><span class="line"><span class="comment">// Number.MIN_VALUE 为Js所能表示的最接近0的数，即大于0的最小数值</span></span><br></pre></td></tr></table></figure>

<p>如果超过了<code>Number.MAX_VALUE</code>的范围，则返回<code>infinity</code>( 正无穷 )或<code>-infinity</code>( 负无穷 )</p>
<h3 id="NaN"><a href="#NaN" class="headerlink" title="NaN"></a>NaN</h3><p>NaN（Not a Number）是一个特殊的数值，js 中如果数值计算没有结果，则返回NaN</p>
<h3 id="其他进制数"><a href="#其他进制数" class="headerlink" title="其他进制数"></a>其他进制数</h3><ul>
<li>16进制：以0x开头</li>
<li>8进制：以0开头</li>
<li>2进制：以0b开头</li>
</ul>
<h2 id="布尔值（Boolean）"><a href="#布尔值（Boolean）" class="headerlink" title="布尔值（Boolean）"></a>布尔值（Boolean）</h2><p>布尔值主要用在条件判断中</p>
<p>它只有两个值 true( 真 ) 或 false( 假 )</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> x = <span class="literal">true</span>;</span><br><span class="line"><span class="keyword">var</span> y = <span class="literal">false</span>;</span><br></pre></td></tr></table></figure>

<h2 id="typeof（检测数据类型）"><a href="#typeof（检测数据类型）" class="headerlink" title="typeof（检测数据类型）"></a>typeof（检测数据类型）</h2><p>功能描述：typeof 是 JavaScript 中的运算符，用来查看变量的数据类型，并返回类型字符串</p>
<p>语法：<code>typeof &lt;值&gt;</code></p>
<p>输出：</p>
<ul>
<li><p>typeof %数值%（返回值：number）</p>
</li>
<li><p>typeof %字符串%（返回值：string）</p>
</li>
<li><p>typeof %布尔值%（返回值：boolean）</p>
</li>
<li><p>typeof undefined（返回值：undefined）</p>
</li>
<li><p>typeof null（返回值：object）</p>
</li>
</ul>
<h2 id="数据类型转换"><a href="#数据类型转换" class="headerlink" title="数据类型转换"></a>数据类型转换</h2><p>使用<code>String()</code>函数 可以将其它数据类型转换为字符串</p>
<p><strong>转换关系：</strong></p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="string">&quot;Hello_你好&quot;</span> =&gt; <span class="string">&quot;Hello_你好&quot;</span>;</span><br><span class="line"><span class="number">123</span> =&gt; <span class="string">&quot;123&quot;</span>;</span><br><span class="line"><span class="function"><span class="params">true</span> =&gt;</span> <span class="string">&quot;true&quot;</span>;</span><br><span class="line"><span class="function"><span class="params">NaN</span> =&gt;</span> <span class="string">&quot;NaN&quot;</span>;</span><br><span class="line"><span class="function"><span class="params">undefined</span> =&gt;</span> <span class="string">&quot;undefined&quot;</span>;</span><br></pre></td></tr></table></figure>

<p>使用<code>Number()</code>函数 可以将其它数据类型转换为数值型</p>
<p><strong>转换关系：</strong></p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="number">123</span> =&gt; <span class="number">123</span>;</span><br><span class="line"><span class="string">&quot;Hello_你好&quot;</span> =&gt; <span class="literal">NaN</span>;</span><br><span class="line"><span class="function"><span class="params">true</span> =&gt;</span> <span class="number">1</span>;</span><br><span class="line"><span class="function"><span class="params">false</span> =&gt;</span> <span class="number">0</span>;</span><br><span class="line"><span class="function"><span class="params">null</span> =&gt;</span> <span class="number">0</span>;</span><br><span class="line"><span class="function"><span class="params">undefined</span> =&gt;</span> <span class="literal">NaN</span>;</span><br></pre></td></tr></table></figure>

<p>使用<code>parseInt()</code>函数可以把一个字符串转换为一个整数（取整）</p>
<p>例：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="built_in">console</span>.log(<span class="built_in">parseInt</span>(<span class="string">&#x27;123a123&#x27;</span>)) <span class="comment">//输出为 123</span></span><br><span class="line"><span class="built_in">console</span>.log(<span class="built_in">parseInt</span>(<span class="string">&#x27;a123&#x27;</span>)) <span class="comment">//输出为 NaN</span></span><br></pre></td></tr></table></figure>

<p>使用<code>parseFloat()</code>函数可以把一个字符串转换为一个浮点数（小数）</p>
<p>例：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> a = <span class="built_in">parseFloat</span>(<span class="string">&#x27;123.5px&#x27;</span>);</span><br><span class="line"><span class="built_in">console</span>.log(a) <span class="comment">//输出为 123.5</span></span><br></pre></td></tr></table></figure>

<p>使用<code>Boolean()</code>函数 可以将其它数据类型转换为布尔型</p>
<p><strong>转换关系：</strong></p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">±<span class="number">123</span> =&gt; <span class="literal">true</span>;</span><br><span class="line"><span class="number">0</span> =&gt; <span class="literal">false</span>;</span><br><span class="line"><span class="function"><span class="params">NaN</span> =&gt;</span> <span class="literal">false</span>;</span><br><span class="line"><span class="function"><span class="params">undefined</span> =&gt;</span> <span class="literal">false</span>;</span><br><span class="line"><span class="string">&quot;Hello_你好&quot;</span> =&gt; <span class="literal">true</span>;</span><br><span class="line"><span class="string">&quot;&quot;</span> =&gt; <span class="literal">false</span>;</span><br><span class="line"><span class="function"><span class="params">null</span> =&gt;</span> <span class="literal">false</span>;</span><br></pre></td></tr></table></figure>]]></content>
      <categories>
        <category>Web开发</category>
      </categories>
      <tags>
        <tag>JavaScript</tag>
      </tags>
  </entry>
  <entry>
    <title>JavaScript(五) 对象与数组</title>
    <url>/posts/3c330ff4/</url>
    <content><![CDATA[<link rel="stylesheet" class="aplayer-secondary-style-marker" href="/assets/css/APlayer.min.css"><script src="/assets/js/APlayer.min.js" class="aplayer-secondary-script-marker"></script><h2 id="对象-（object）"><a href="#对象-（object）" class="headerlink" title="对象 （object）"></a>对象 （object）</h2><p>一个对象就是一系列属性的集合，一个属性包含一个名和一个值</p>
<p>例：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">/* 对象字面量 */</span></span><br><span class="line">&#123;</span><br><span class="line">  <span class="attr">name</span>: <span class="string">&#x27;zs&#x27;</span>,</span><br><span class="line">  <span class="attr">age</span>: <span class="number">18</span>,</span><br><span class="line">  <span class="attr">sex</span>: <span class="literal">true</span>,</span><br><span class="line">  <span class="attr">sayHi</span>: &#123;</span><br><span class="line">    <span class="attr">first</span>: <span class="string">&#x27;Bob&#x27;</span>,</span><br><span class="line">    <span class="attr">last</span>: <span class="string">&#x27;Smith&#x27;</span></span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>对象是由一系列属性组成，每一个属性都有一个名字（如上面案例中的 name、age），和一个值（如上面案例中的 ‘zs’、18）。每一个名/值对之间用逗号( <code>,</code> )分隔，并且名和值之间由冒号( <code>:</code> )分隔</p>
<p>对象中的属性值可以是任何类型的数据( 包括对象 )</p>
<h3 id="创建对象"><a href="#创建对象" class="headerlink" title="创建对象"></a>创建对象</h3><p><strong>使用 对象字面量 创建一个对象</strong></p>
<p>语法：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> 对象名 = 对象字面量</span><br></pre></td></tr></table></figure>

<p>例：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> obj = &#123;&#125;	<span class="comment">//创建一个名为obj的对象</span></span><br><span class="line"><span class="built_in">console</span>.log(<span class="keyword">typeof</span> obj); <span class="comment">// &#x27;object&#x27;</span></span><br><span class="line"><span class="comment">/* 创建一个名为obj2的对象，对象中包含两个属性 */</span></span><br><span class="line"><span class="keyword">var</span> obj2 = &#123;</span><br><span class="line">  <span class="attr">name</span>: <span class="string">&#x27;Tom&#x27;</span>,</span><br><span class="line">  <span class="attr">age</span>: <span class="number">18</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="访问对象表达式"><a href="#访问对象表达式" class="headerlink" title="访问对象表达式"></a>访问对象表达式</h3><p><strong>1、点表示法</strong></p>
<p>语法：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">对象.属性名</span><br></pre></td></tr></table></figure>

<p>例：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">person;					<span class="comment">//返回person对象</span></span><br><span class="line">person.name;		<span class="comment">//返回person对象中name属性的值</span></span><br><span class="line">person.age = <span class="number">18</span>;	<span class="comment">//向person对象中的name属性赋值</span></span><br></pre></td></tr></table></figure>

<p><strong>2、括号表示法</strong></p>
<p>语法：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">对象[属性名]</span><br></pre></td></tr></table></figure>

<blockquote>
<h3 id="属性与变量"><a href="#属性与变量" class="headerlink" title="属性与变量"></a>属性与变量</h3><p>对象中的属性和变量都可以用来存储任意类型的数据，并且它们的值都是可以更变的</p>
<ul>
<li>当向对象中的属性赋值时，如果对象中不包含该属性，则会自动添加该属性并赋值</li>
</ul>
<h3 id="点与括号"><a href="#点与括号" class="headerlink" title="点与括号"></a>点与括号</h3><p>点和括号都可以用来分隔对象的名称字段，在对象中，属性名可以是任何字符</p>
<ul>
<li>点表示法只能使用合法字符作为属性名，而括号表示法可以将任何表达式的值作为属性名</li>
<li>如果属性名为不合法( 与 Js 语法相冲突 )的标识符，则必须用括号表示法</li>
</ul>
<p>例：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">/* 例1 */</span></span><br><span class="line"><span class="keyword">var</span> obj = &#123;</span><br><span class="line">  [<span class="string">&#x27;.+-*/&amp;|!&#x27;</span>]: <span class="number">1234</span></span><br><span class="line">&#125;</span><br><span class="line"><span class="built_in">console</span>.log(obj[<span class="string">&#x27;.+-*/&amp;|!&#x27;</span>])	<span class="comment">//返回1234</span></span><br><span class="line"><span class="comment">/* 例2 */</span></span><br><span class="line"><span class="keyword">var</span> o1 = <span class="string">&#x27;name&#x27;</span>;</span><br><span class="line"><span class="keyword">var</span> obj2 = &#123;</span><br><span class="line">  [o1]: <span class="string">&#x27;Joy&#x27;</span>	<span class="comment">//将变量o1的值作为属性名</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>在实际开发中对象的属性名尽量按照标识符的规范命名</p>
</blockquote>
<h3 id="delete-运算符"><a href="#delete-运算符" class="headerlink" title="delete 运算符"></a>delete 运算符</h3><p>当要删除一个对象中的属性时，可以使用 delete 运算符</p>
<p>语法：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">delete</span> 对象.属性名</span><br><span class="line"> or</span><br><span class="line"><span class="keyword">delete</span> 对象[属性名]</span><br></pre></td></tr></table></figure>

<p>例：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> obj = &#123;</span><br><span class="line">  <span class="attr">one</span>: <span class="number">1</span></span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">delete</span> obj.one;</span><br><span class="line"><span class="built_in">console</span>.log(obj);	<span class="comment">//输出为 &#123;&#125; 空对象</span></span><br></pre></td></tr></table></figure>

<h2 id="数组（array）"><a href="#数组（array）" class="headerlink" title="数组（array）"></a>数组（array）</h2><p>数组是一个<strong>有序</strong>的数据集合，通常被描述为“像列表一样的对象”</p>
<p>例：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">/* 数组字面量 */</span></span><br><span class="line">[</span><br><span class="line">  <span class="number">0</span>,</span><br><span class="line">  <span class="literal">true</span>,</span><br><span class="line">  <span class="string">&#x27;hello&#x27;</span>,</span><br><span class="line">	[<span class="number">1</span>,<span class="number">2</span>,<span class="number">3</span>],</span><br><span class="line">  &#123;<span class="attr">name</span>: <span class="string">&#x27;Joy&#x27;</span>&#125;</span><br><span class="line">]</span><br></pre></td></tr></table></figure>

<p>数组由一系列的值组成，每一个值被称作一个元素( 元素可以是任何类型的数据，包括对象 )，且值与值之间用( <code>,</code> )分隔</p>
<h3 id="创建数组"><a href="#创建数组" class="headerlink" title="创建数组"></a>创建数组</h3><p>语法：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> 数组名 = 数组字面量</span><br></pre></td></tr></table></figure>

<p>例：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> arr = [];	<span class="comment">//创建一个名为arr的数组</span></span><br><span class="line"><span class="comment">/* 创建一个名为arr2的数组，数组中包含3个元素 */</span></span><br><span class="line"><span class="keyword">var</span> arr2 = [ <span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span> ];</span><br></pre></td></tr></table></figure>

<h3 id="访问数组表达式"><a href="#访问数组表达式" class="headerlink" title="访问数组表达式"></a>访问数组表达式</h3><p>语法：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">数组[索引]</span><br></pre></td></tr></table></figure>

<p>例：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">list			<span class="comment">//返回list数组</span></span><br><span class="line">list[<span class="number">0</span>];	<span class="comment">//返回list数组中第1个元素的值</span></span><br><span class="line">list[<span class="number">0</span>] = <span class="number">1</span>;<span class="comment">//向list数组中的第1个元素赋值</span></span><br></pre></td></tr></table></figure>

<blockquote>
<h3 id="索引"><a href="#索引" class="headerlink" title="索引"></a>索引</h3><p>数组中每一个元素都有一个数字编号称为索引，索引是从0开始的整数，通过索引可以访问数组元素的值</p>
<h3 id="数组也是对象"><a href="#数组也是对象" class="headerlink" title="数组也是对象"></a>数组也是对象</h3><p>数组用<code>[]</code>表示，对象用<code>&#123;&#125;</code>表示。数组和对象一样，都可以添加属性，区别是数组中如果属性名为整数字符，则会将其作为数组元素( 有序数据 )来表示</p>
<p>使用 typeof 检查数组数据类型时，返回 ‘object’</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> arr = [];</span><br><span class="line"><span class="keyword">typeof</span> arr <span class="comment">// &#x27;object&#x27;</span></span><br></pre></td></tr></table></figure>
</blockquote>
<h3 id="数组的实例属性和方法"><a href="#数组的实例属性和方法" class="headerlink" title="数组的实例属性和方法"></a>数组的实例属性和方法</h3><p><strong>属性：</strong></p>
<ul>
<li><a href="https://www.w3school.com.cn/jsref/jsref_length_array.asp"><code>.length</code></a> 返回或更改数组的长度</li>
</ul>
<p><strong>方法：</strong></p>
<table>
<thead>
<tr>
<th>方法</th>
<th>描述</th>
<th>原数组</th>
</tr>
</thead>
<tbody><tr>
<td><a href="https://www.w3school.com.cn/jsref/jsref_concat_array.asp">.concat()</a></td>
<td>将两个或多个数组连接合并为一个数组，并返回合并后的数组</td>
<td>不改变</td>
</tr>
<tr>
<td><a href="https://www.w3school.com.cn/jsref/jsref_join.asp">.join()</a></td>
<td>将一个数组的所有元素连接成一个字符串，并返回这个字符串</td>
<td>不改变</td>
</tr>
<tr>
<td><a href="https://www.w3school.com.cn/jsref/jsref_pop.asp">.pop()</a></td>
<td>删除并返回数组中最后一个元素</td>
<td>改变</td>
</tr>
<tr>
<td><a href="https://www.w3school.com.cn/jsref/jsref_shift.asp">.shift()</a></td>
<td>删除并返回数组中的第一个元素</td>
<td>改变</td>
</tr>
<tr>
<td><a href="https://www.w3school.com.cn/jsref/jsref_push.asp">.push()</a></td>
<td>将一个或多个元素添加到数组的末尾，并返回该数组的新长度(<code>.length</code>)</td>
<td>改变</td>
</tr>
<tr>
<td><a href="https://www.w3school.com.cn/jsref/jsref_unshift.asp">.unshift()</a></td>
<td>将一个或多个元素添加到数组的开头，并返回该数组的新长度(<code>.length</code>)</td>
<td>改变</td>
</tr>
<tr>
<td><a href="https://www.w3school.com.cn/jsref/jsref_sort.asp">.sort()</a></td>
<td>对数组的元素进行排序，并返回排列后的数组</td>
<td>改变</td>
</tr>
<tr>
<td><a href="https://www.w3school.com.cn/jsref/jsref_reverse.asp">.reverse()</a></td>
<td>颠倒数组中元素的顺序，并返回颠倒后的数组</td>
<td>改变</td>
</tr>
<tr>
<td><a href="https://www.w3school.com.cn/jsref/jsref_slice_array.asp">.slice()</a></td>
<td>提取数组中指定的元素，并将提取出的元素作为新的数组返回</td>
<td>不改变</td>
</tr>
<tr>
<td><a href="https://www.w3school.com.cn/jsref/jsref_splice.asp">.splice()</a></td>
<td>删除或替换数组中指定的元素，并将删除或替换后的数组返回</td>
<td>改变</td>
</tr>
<tr>
<td><a href="https://www.w3school.com.cn/jsref/jsref_indexof_array.asp">.indexOf()</a></td>
<td>查找数组中是否包含指定的元素，如果不包含则返回<code>-1</code>，包含则返回该元素的索引值</td>
<td>不改变</td>
</tr>
<tr>
<td></td>
<td></td>
<td></td>
</tr>
</tbody></table>
<blockquote>
<h3 id="in-运算符"><a href="#in-运算符" class="headerlink" title="in 运算符"></a>in 运算符</h3><p>in运算符可以检测一个属性是否在指定的对象中，如果在则返回 true，不在则返回 false</p>
<p>语法：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">属性 <span class="keyword">in</span> 对象名 <span class="comment">//属性可以是属性名( 字符串 ) 或 数组索引( 数字 )</span></span><br></pre></td></tr></table></figure>

<p>例：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> obj = &#123;</span><br><span class="line">	<span class="attr">a</span>: <span class="number">1</span>,</span><br><span class="line">	<span class="attr">b</span>: <span class="number">2</span>,</span><br><span class="line">	<span class="attr">c</span>: <span class="number">3</span></span><br><span class="line">&#125;</span><br><span class="line"><span class="built_in">console</span>.log(<span class="string">&#x27;a&#x27;</span> <span class="keyword">in</span> obj, <span class="string">&#x27;x&#x27;</span> <span class="keyword">in</span> obj); <span class="comment">//输出：true false</span></span><br></pre></td></tr></table></figure>

<h3 id="for…in（枚举对象）"><a href="#for…in（枚举对象）" class="headerlink" title="for…in（枚举对象）"></a>for…in（枚举对象）</h3><p><strong><code>for...in</code>语句</strong>用来枚举( 循环遍历 )对象中的属性</p>
<p>语法：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">for</span> (变量 <span class="keyword">in</span> 对象名) &#123;</span><br><span class="line">	<span class="comment">//循环体(语句)</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>例：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> obj = &#123;<span class="attr">a</span>:<span class="number">1</span>, <span class="attr">b</span>:<span class="number">2</span>, <span class="attr">c</span>:<span class="number">3</span>&#125;;</span><br><span class="line"><span class="keyword">for</span> (<span class="keyword">var</span> prop <span class="keyword">in</span> obj) &#123;</span><br><span class="line"><span class="built_in">console</span>.log(<span class="string">&quot;obj.&quot;</span> + prop + <span class="string">&quot; = &quot;</span> + obj[prop]);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>PS：当 in运算符在 for语句中使用时，其作用就会增加( 增加了赋值和迭代的作用 )</p>
<p><strong>执行步骤：</strong></p>
<p>在 <code>for...in</code> 语句中，in运算符会先将对象中的第1个属性名赋值到左侧变量中，赋值完成后就会执行一次循环体，之后跳回赋值阶段赋值第2个属性名，并再次执行循环体，以此不断循环直至循环到最后一个属性名为止</p>
</blockquote>
<h2 id="原始值和引用值"><a href="#原始值和引用值" class="headerlink" title="原始值和引用值"></a>原始值和引用值</h2><p>JavaScript 基本数据类型在语言最底层面表现为不可变值，这些值被称为原始值，如：数值，布尔值等…</p>
<p>JavaScript 中 像对象、数组和函数都被称为引用值，它在语言最底层面表现为可变值</p>
<p>这里的可变值是指无需创建新值即可更改的值，而不可变值是指如果不创建新的值，就无法更改其数据的值</p>
<p>ECMAScript 标准中指定原始值存储在栈内存中，而引用值存储在堆内存中。栈内存和堆内存代表了 2 种不同的存储方式</p>
<blockquote>
<h3 id="栈内存-stack-memory"><a href="#栈内存-stack-memory" class="headerlink" title="栈内存( stack memory )"></a>栈内存( stack memory )</h3><ul>
<li>栈内存是一个连续的内存空间，栈内存遵循 <em>FILO</em> 的原则，即 <em>后进先出</em> 的原则</li>
<li>栈内存用来存储大小固定的数据，这些数据在编译时大小就被确定</li>
<li>在 JavaScript 中，栈内存主要被用来存储函数执行上下文及其变量</li>
<li>栈内存的数据存取速度要高于堆内存</li>
</ul>
<h3 id="堆内存-heap-memory"><a href="#堆内存-heap-memory" class="headerlink" title="堆内存( heap memory )"></a>堆内存( heap memory )</h3><ul>
<li>堆内存是一种非连续的存储结构，通常采用链表或树形结构进行管理</li>
<li>堆内存中每个数据项都保存了指向其它数据项的地址信息，这使得堆内存的存取非常灵活</li>
<li>堆内存空间是在运行时动态分配的，用于存储一些大小不确定的数据</li>
<li>当堆内存中的某个数据不再被引用时，它所占用的内存会被 JavaScript 引擎中的垃圾回收器自动释放。垃圾回收器会根据一定的算法来判断哪些数据没有被引用，然后释放这些数据所占用的内存</li>
</ul>
</blockquote>
<p>例：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> a = <span class="number">123</span>;</span><br><span class="line"><span class="keyword">var</span> b = a;</span><br><span class="line">a = <span class="number">456</span>;</span><br></pre></td></tr></table></figure>

<p>例图：</p>
<p><img src="https://cdn3.tianli0.top/gh/k34869/images/s/20230503105219" alt="img01"></p>
<p>当创建一个对象时，会先在堆内存中分配一块内存空间来存储对象，而栈内存则存储的是该对象所在堆内存的内存地址（引用值）</p>
<p>例图：</p>
<p><img src="https://cdn3.tianli0.top/gh/k34869/images/s/20230211111728" alt="img02"></p>
<p>例：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> obj1 = &#123;</span><br><span class="line">  <span class="attr">name</span>: <span class="string">&#x27;张三&#x27;</span></span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">var</span> obj2 = obj1;</span><br><span class="line">obj2.name = <span class="string">&quot;李四&quot;</span>;</span><br><span class="line"><span class="built_in">console</span>.log(obj1.name);	<span class="comment">//输出：&#x27;李四&#x27;</span></span><br><span class="line"><span class="built_in">console</span>.log(obj2.name);	<span class="comment">//输出：&#x27;李四&#x27;</span></span><br></pre></td></tr></table></figure>]]></content>
      <categories>
        <category>Web开发</category>
      </categories>
      <tags>
        <tag>JavaScript</tag>
      </tags>
  </entry>
  <entry>
    <title>JavaScript(八) 内置对象</title>
    <url>/posts/2cfcc5d5/</url>
    <content><![CDATA[<link rel="stylesheet" class="aplayer-secondary-style-marker" href="/assets/css/APlayer.min.css"><script src="/assets/js/APlayer.min.js" class="aplayer-secondary-script-marker"></script><p>内置对象( native object )是 ECMAScript 标准中定义的对象（标准内置对象）。内置对象独立与浏览器在任何 <strong>ECMAScript实现</strong> 中都可以使用</p>
<h2 id="继承链"><a href="#继承链" class="headerlink" title="继承链"></a>继承链</h2><table>
<thead>
<tr>
<th>内置构造函数( 类 )</th>
<th>描述</th>
<th><code>__proto__</code>(原型)</th>
</tr>
</thead>
<tbody><tr>
<td>Object 类型</td>
<td>Js中所有的对象都是<code>Object</code>类型的实例，它们都会从<code>Object.prototype</code>继承属性和方法(如：<code>toString()</code>)</td>
<td><code>Function.prototype</code></td>
</tr>
<tr>
<td>Function 类型</td>
<td>Js中所有的函数都是<code>Function</code>类型的实例，它们都会从<code>Function.prototype</code>继承属性和方法(如：<code>apply()</code>、<code>call()</code>等)</td>
<td><code>Function.prototype</code></td>
</tr>
<tr>
<td>Array 类型</td>
<td>Js中所有的数组都是<code>Array</code>类型的实例，它们都会从<code>Array.prototype</code>继承属性和方法(如：<code>length</code>、<code>push()</code>、<code>concat()</code>等)</td>
<td><code>Function.prototype</code></td>
</tr>
</tbody></table>
<blockquote>
<h3 id="对象的创建"><a href="#对象的创建" class="headerlink" title="对象的创建"></a>对象的创建</h3><p><strong>创建一个Array类型的对象</strong>（即创建一个数组）</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">/* 语法 */</span></span><br><span class="line"><span class="keyword">var</span> 数组名 = <span class="keyword">new</span> <span class="built_in">Array</span>(元素<span class="number">1</span>,元素<span class="number">2.</span>..元素N);</span><br><span class="line"><span class="comment">/* 例 */</span></span><br><span class="line"><span class="keyword">var</span> arr = <span class="keyword">new</span> <span class="built_in">Array</span>(<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>);</span><br></pre></td></tr></table></figure>

<p><strong>创建一个Function类型的对象</strong>（即创建一个函数）</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">/* 语法 */</span></span><br><span class="line"><span class="keyword">var</span> 函数名 = <span class="keyword">new</span> <span class="built_in">Function</span>(语句字符串);</span><br><span class="line"><span class="comment">/* 例 */</span></span><br><span class="line"><span class="keyword">var</span> fun = <span class="keyword">new</span> <span class="built_in">Function</span>(<span class="string">&#x27;console.log(&quot;hello&quot;)&#x27;</span>);</span><br></pre></td></tr></table></figure>

<p><strong>创建一个Object类型的对象</strong>（即创建一个对象）</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">/* 语法 */</span></span><br><span class="line"><span class="keyword">var</span> 对象名 = <span class="keyword">new</span> <span class="built_in">Object</span>(表达式); <span class="comment">//参数默认值为null即空值</span></span><br><span class="line"><span class="comment">/* 例1 */</span></span><br><span class="line"><span class="keyword">var</span> obj = <span class="keyword">new</span> <span class="built_in">Object</span>(&#123;<span class="attr">name</span>: Timeic&#125;); <span class="comment">//创建一个Object类型的对象</span></span><br><span class="line"><span class="comment">/* 通过Object类来创建其他类型的对象 */</span></span><br><span class="line"><span class="keyword">var</span> arr = <span class="keyword">new</span> <span class="built_in">Object</span>([<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>]); <span class="comment">//创建Array类型对象</span></span><br><span class="line"><span class="keyword">var</span> fun = <span class="keyword">new</span> <span class="built_in">Object</span>(<span class="function"><span class="keyword">function</span>(<span class="params"></span>)</span>&#123;<span class="built_in">console</span>.log(<span class="string">&#x27;hello&#x27;</span>)&#125;); <span class="comment">//创建Function类型对象</span></span><br><span class="line"><span class="keyword">var</span> num = <span class="keyword">new</span> <span class="built_in">Object</span>(<span class="number">666</span>); <span class="comment">//创建一个Number类型对象</span></span><br><span class="line"><span class="keyword">var</span> str = <span class="keyword">new</span> <span class="built_in">Object</span>(<span class="string">&#x27;hello&#x27;</span>); <span class="comment">//创建一个String类型对象</span></span><br><span class="line"><span class="keyword">var</span> bool = <span class="keyword">new</span> <span class="built_in">Object</span>(<span class="literal">true</span>); <span class="comment">//创建一个Boolean类型对象</span></span><br></pre></td></tr></table></figure>

<p>在JavaScript中字面量的创建方式只是实例化的一种简化语法</p>
<p>如：<code>var obj = &#123;&#125;</code> 等同于 <code>var obj = new Object()</code></p>
<p>如果不赋值只实例化则会创建一个匿名的( 临时的 )实例对象</p>
<p>如：<code>(&#123;name: &#39;Timeic&#39;&#125;).name</code> or <code>[1, 2, 3].length</code></p>
</blockquote>
<h3 id="包装类型"><a href="#包装类型" class="headerlink" title="包装类型"></a>包装类型</h3><p>在 Js 中有三种包装类型对象，分别是Number、String、Boolean</p>
<table>
<thead>
<tr>
<th>类</th>
<th><code>__proto__</code>(原型)</th>
</tr>
</thead>
<tbody><tr>
<td>Number 类型</td>
<td><code>Function.prototype</code></td>
</tr>
<tr>
<td>String 类型</td>
<td><code>Function.prototype</code></td>
</tr>
<tr>
<td>Boolean 类型</td>
<td><code>Function.prototype</code></td>
</tr>
</tbody></table>
<p>例：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> num = <span class="keyword">new</span> <span class="built_in">Number</span>(<span class="number">666</span>); <span class="comment">//创建一个Number类型的对象</span></span><br><span class="line"><span class="keyword">var</span> str = <span class="keyword">new</span> <span class="built_in">String</span>(<span class="string">&#x27;hello&#x27;</span>); <span class="comment">//创建一个String类型的对象</span></span><br><span class="line"><span class="keyword">var</span> bool = <span class="keyword">new</span> <span class="built_in">Boolean</span>(<span class="literal">true</span>); <span class="comment">//创建一个Boolean类型的对象</span></span><br></pre></td></tr></table></figure>

<p>Number、String、Boolean分别对应三种基本数据类型，即数值型、字符串、布尔型。这三种基本数据类型可以像对象一样访问属性或调用方法，当访问某个属性时 js 引擎会自动创建一个对应的匿名的( 临时的 )包装类型对象，从该对象中获取属性和方法</p>
<p>如：<code>&#39;abc&#39;.length</code> or <code>true.toString()</code></p>
<h2 id="Date-对象"><a href="#Date-对象" class="headerlink" title="Date 对象"></a>Date 对象</h2><p>Date 对象用于处理日期与时间，通过<code>new Date()</code>来创建一个Date类型的实例对象，创建的Date实例对象会显示实例化时的日期和时间</p>
<p>例：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">//创建Date类型的实例对象</span></span><br><span class="line"><span class="keyword">var</span> d = <span class="keyword">new</span> <span class="built_in">Date</span>()</span><br><span class="line"><span class="built_in">console</span>.log(d) <span class="comment">//Sat May 20 2022 17:40:41 GMT+0800 (中国标准时间)</span></span><br></pre></td></tr></table></figure>

<p>方法：</p>
<table>
<thead>
<tr>
<th>方法</th>
<th>描述</th>
</tr>
</thead>
<tbody><tr>
<td><a href="https://www.w3school.com.cn/jsref/jsref_getFullYear.asp">getFullYear()</a></td>
<td>返回一个指定的Date对象的完整年份（四位数年份）</td>
</tr>
<tr>
<td><a href="https://www.w3school.com.cn/jsref/jsref_getMonth.asp">getMonth()</a></td>
<td>返回一个指定的Date对象为第几月</td>
</tr>
<tr>
<td><a href="https://www.w3school.com.cn/jsref/jsref_getDate.asp">getDate()</a></td>
<td>返回一个指定的Date对象为一个月中的哪一日（<code>1</code>-<code>31</code>）</td>
</tr>
<tr>
<td><a href="https://www.w3school.com.cn/jsref/jsref_getDay.asp">getDay()</a></td>
<td>返回一个指定的Date对象为一周中的第几天，（<code>0</code>-<code>6</code>），0 表示星期天</td>
</tr>
<tr>
<td><a href="https://www.w3school.com.cn/jsref/jsref_getHours.asp">getHours()</a></td>
<td>返回一个指定的Date对象的小时（<code>0</code>–<code>23</code>）</td>
</tr>
<tr>
<td><a href="https://www.w3school.com.cn/jsref/jsref_getMinutes.asp">getMinutes()</a></td>
<td>返回一个指定的Date对象的分钟（<code>0</code>–<code>59</code>）</td>
</tr>
<tr>
<td><a href="https://www.w3school.com.cn/jsref/jsref_getSeconds.asp">getSeconds()</a></td>
<td>返回一个指定的Date对象的秒（<code>0</code>–<code>59</code>）</td>
</tr>
<tr>
<td><a href="https://www.w3school.com.cn/jsref/jsref_getMilliseconds.asp">getMilliseconds()</a></td>
<td>返回一个指定的Date对象的秒（<code>0</code>–<code>999</code>）</td>
</tr>
</tbody></table>
<h2 id="Math-对象"><a href="#Math-对象" class="headerlink" title="Math 对象"></a>Math 对象</h2><p>Math 对象用于执行数学任务，Math对象不需要创建实例，直接使用即可</p>
<p>静态属性（常量）：</p>
<table>
<thead>
<tr>
<th>属性</th>
<th>描述</th>
</tr>
</thead>
<tbody><tr>
<td><a href="https://www.w3school.com.cn/jsref/jsref_e.asp">E</a></td>
<td>返回算术常量 e，即自然对数的底数（约等于2.718）</td>
</tr>
<tr>
<td><a href="https://www.w3school.com.cn/jsref/jsref_ln2.asp">LN2</a></td>
<td>返回 2 的自然对数（约等于0.693）</td>
</tr>
<tr>
<td><a href="https://www.w3school.com.cn/jsref/jsref_ln10.asp">LN10</a></td>
<td>返回 10 的自然对数（约等于2.302）</td>
</tr>
<tr>
<td><a href="https://www.w3school.com.cn/jsref/jsref_log2e.asp">LOG2E</a></td>
<td>返回以 2 为底的 e 的对数（约等于 1.414）</td>
</tr>
<tr>
<td><a href="https://www.w3school.com.cn/jsref/jsref_log10e.asp">LOG10E</a></td>
<td>返回以 10 为底的 e 的对数（约等于0.434）</td>
</tr>
<tr>
<td><a href="https://www.w3school.com.cn/jsref/jsref_pi.asp">PI</a></td>
<td>返回圆周率（约等于3.14159）</td>
</tr>
</tbody></table>
<p>静态方法：</p>
<table>
<thead>
<tr>
<th>方法</th>
<th>描述</th>
</tr>
</thead>
<tbody><tr>
<td><a href="https://www.w3school.com.cn/jsref/jsref_abs.asp">abs(x)</a></td>
<td>返回数的绝对值</td>
</tr>
<tr>
<td><a href="https://www.w3school.com.cn/jsref/jsref_ceil.asp">ceil(x)</a></td>
<td>对数进行上舍入</td>
</tr>
<tr>
<td><a href="https://www.w3school.com.cn/jsref/jsref_floor.asp">floor(x)</a></td>
<td>对数进行下舍入</td>
</tr>
<tr>
<td><a href="https://www.w3school.com.cn/jsref/jsref_max.asp">max(x, y)</a></td>
<td>返回 x 和 y 中的最高值</td>
</tr>
<tr>
<td><a href="https://www.w3school.com.cn/jsref/jsref_min.asp">min(x, y)</a></td>
<td>返回 x 和 y 中的最低值</td>
</tr>
<tr>
<td><a href="https://www.w3school.com.cn/jsref/jsref_pow.asp">pow(x, y)</a></td>
<td>返回 x 的 y 次幂</td>
</tr>
<tr>
<td><a href="https://www.w3school.com.cn/jsref/jsref_random.asp">random()</a></td>
<td>返回 0 ~ 1 之间的随机数</td>
</tr>
<tr>
<td><a href="https://www.w3school.com.cn/jsref/jsref_round.asp">round(x)</a></td>
<td>把数四舍五入为最接近的整数</td>
</tr>
<tr>
<td><a href="https://www.w3school.com.cn/jsref/jsref_sqrt.asp">sqrt(x)</a></td>
<td>返回数的平方根</td>
</tr>
</tbody></table>
<h2 id="String-对象"><a href="#String-对象" class="headerlink" title="String 对象"></a>String 对象</h2><p>String 类型对象是一个包含一组字符序列的对象</p>
<p>语法：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> str = <span class="keyword">new</span> <span class="built_in">String</span>(字符串); <span class="comment">//创建一个String类型对象</span></span><br><span class="line"><span class="built_in">String</span>(s); <span class="comment">//将参数 s 的值转换成字符串数据类型</span></span><br></pre></td></tr></table></figure>

<p>例：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> str = <span class="keyword">new</span> <span class="built_in">String</span>(<span class="string">&#x27;hello&#x27;</span>);</span><br><span class="line"><span class="built_in">console</span>.log(str[<span class="number">4</span>]); <span class="comment">//输出：&#x27;o&#x27;</span></span><br><span class="line"><span class="built_in">console</span>.log(<span class="string">&#x27;hello&#x27;</span>.replace(<span class="string">&#x27;h&#x27;</span>, <span class="string">&#x27;H&#x27;</span>)) <span class="comment">//输出：&#x27;Hello&#x27;</span></span><br></pre></td></tr></table></figure>

<p>属性：</p>
<ul>
<li><p><strong><code>[下标]</code> 根据索引返回指定位置的一个字符</strong></p>
</li>
<li><p><code>length</code> <strong>获取字符串长度</strong></p>
</li>
</ul>
<p>方法：</p>
<table>
<thead>
<tr>
<th>方法</th>
<th>描述</th>
<th>原值</th>
</tr>
</thead>
<tbody><tr>
<td><a href="https://www.w3school.com.cn/jsref/jsref_charAt.asp">charAt()</a></td>
<td>根据索引，返回在指定位置的字符</td>
<td>不变</td>
</tr>
<tr>
<td><a href="https://www.w3school.com.cn/jsref/jsref_charCodeAt.asp">charCodeAt()</a></td>
<td>根据索引返回指定位置字符的 Unicode 编码（十进制）</td>
<td>不变</td>
</tr>
<tr>
<td><a href="https://www.w3school.com.cn/jsref/jsref_fromCharCode.asp">String.formCharCode()</a></td>
<td>根据Unicode编码创建一个字符串</td>
<td>不变(静态方法)</td>
</tr>
<tr>
<td><a href="https://www.w3school.com.cn/jsref/jsref_concat_string.asp">concat()</a></td>
<td>连接多个字符串</td>
<td>不变</td>
</tr>
<tr>
<td><a href="https://www.w3school.com.cn/jsref/jsref_indexOf.asp">indexOf()</a></td>
<td>检索一个字符串中是否含有指定内容，如果有则返回其首次出现的位置(索引)，没有则返回 -1</td>
<td>不变</td>
</tr>
<tr>
<td><a href="https://www.w3school.com.cn/jsref/jsref_lastIndexOf.asp">lastIndexOf()</a></td>
<td>检索一个字符串中是否含有指定内容，如果有则返回其最后出现的位置(索引)，没有则返回 -1</td>
<td>不变</td>
</tr>
<tr>
<td><a href="https://www.w3school.com.cn/jsref/jsref_slice_string.asp">slice()</a></td>
<td>截取字符串中指定的内容并返回</td>
<td>不变</td>
</tr>
<tr>
<td><a href="https://www.w3school.com.cn/jsref/jsref_substring.asp">substring()</a></td>
<td>返回一个字符串在开始索引到结束索引之间的内容</td>
<td>不变</td>
</tr>
<tr>
<td><a href="https://www.w3school.com.cn/jsref/jsref_split.asp">split()</a></td>
<td>将字符串拆分成数组并返回</td>
<td>不变</td>
</tr>
<tr>
<td><a href="https://www.w3school.com.cn/jsref/jsref_toUpperCase.asp">toUpperCase()</a></td>
<td>将字符串转换为大写形式并返回</td>
<td>不变</td>
</tr>
<tr>
<td><a href="https://www.w3school.com.cn/jsref/jsref_toLowerCase.asp">toLowerCase()</a></td>
<td>将字符串转换为小写形式并返回</td>
<td>不变</td>
</tr>
<tr>
<td><a href="https://www.w3school.com.cn/jsref/jsref_search.asp">search()</a></td>
<td>搜索与正则表达式相匹配的值，并返回该值所在位置(索引)</td>
<td>不变</td>
</tr>
<tr>
<td><a href="https://www.w3school.com.cn/jsref/jsref_match.asp">match()</a></td>
<td>查找一个或多个正则表达式的匹配，并返回一个结果数组</td>
<td>不变</td>
</tr>
<tr>
<td><a href="https://www.w3school.com.cn/jsref/jsref_replace.asp">replace()</a></td>
<td>将字符串中指定内容替换为新的内容，并返回替换后内容</td>
<td>不变</td>
</tr>
</tbody></table>
<h2 id="Array-对象"><a href="#Array-对象" class="headerlink" title="Array 对象"></a>Array 对象</h2><h3 id="array-forEach"><a href="#array-forEach" class="headerlink" title="array.forEach()"></a>array.forEach()</h3><p>描述：按顺序为数组中的每个元素调用一次指定的函数</p>
<p>语法：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">数组.forEach(回调函数, 回调函数中<span class="built_in">this</span>的值)</span><br><span class="line"><span class="comment">//回调函数的语法</span></span><br><span class="line"><span class="function"><span class="keyword">function</span>(<span class="params">当前元素值, 当前元素索引, 当前数组</span>)</span>&#123;</span><br><span class="line">  <span class="comment">// xxx</span></span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">/*</span></span><br><span class="line"><span class="comment">	当前元素值：当前迭代的数组元素值</span></span><br><span class="line"><span class="comment">	当前元素索引：当前迭代的数组元素索引值</span></span><br><span class="line"><span class="comment">	当前数组：调用当前方法的数组</span></span><br><span class="line"><span class="comment">*/</span></span><br></pre></td></tr></table></figure>

<p>例：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">/* 例2 */</span></span><br><span class="line"><span class="keyword">var</span> out = [];</span><br><span class="line">[<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>].forEach(<span class="function"><span class="keyword">function</span>(<span class="params">elem</span>) </span>&#123;</span><br><span class="line">  <span class="built_in">this</span>.push(elem * elem);</span><br><span class="line">&#125;, out);</span><br><span class="line"><span class="built_in">console</span>.log(out); <span class="comment">// [1, 4, 9]</span></span><br></pre></td></tr></table></figure>

<blockquote>
<p>PS：<code>forEach()</code> 方法用于循环遍历数组，该方法不返回任何值，同时该方法不能中断执行，它总是将数组元素遍历完才结束，如果想在循环时根据条件中断执行建议使用 <code>for</code> 循环</p>
</blockquote>
<h3 id="array-map"><a href="#array-map" class="headerlink" title="array.map()"></a>array.map()</h3><p>描述：按顺序为数组中的每个元素调用一次指定的函数，并将每一次调用的函数返回值组合成一个数组作为 <code>map()</code> 方法的返回值</p>
<p>语法：同 <code>array.forEach()</code> 方法</p>
<p>例：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">/* 例1 */</span></span><br><span class="line"><span class="keyword">var</span> numbers = [<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>];</span><br><span class="line"><span class="keyword">var</span> newArr = numbers.map(<span class="function"><span class="keyword">function</span> (<span class="params">n</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">return</span> n + <span class="number">1</span>;</span><br><span class="line">&#125;);</span><br><span class="line"><span class="built_in">console</span>.log(newArr) <span class="comment">// [2, 3, 4]</span></span><br><span class="line"><span class="comment">/* 例2 */</span></span><br><span class="line"><span class="keyword">var</span> arr = [<span class="string">&#x27;a&#x27;</span>, <span class="string">&#x27;b&#x27;</span>, <span class="string">&#x27;c&#x27;</span>];</span><br><span class="line"><span class="keyword">var</span> newArr = [<span class="number">1</span>, <span class="number">2</span>].map(<span class="function"><span class="keyword">function</span> (<span class="params">e</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">return</span> <span class="built_in">this</span>[e];</span><br><span class="line">&#125;, arr)</span><br><span class="line"><span class="built_in">console</span>.log(newArr) <span class="comment">// [&#x27;b&#x27;, &#x27;c&#x27;]</span></span><br></pre></td></tr></table></figure>

<blockquote>
<p>PS：<code>map()</code> 方法用于对数组中每个元素进行处理并返回一个新数组</p>
</blockquote>
<h3 id="array-filter"><a href="#array-filter" class="headerlink" title="array.filter()"></a>array.filter()</h3><p>描述：将满足条件的元素组合成一个新数组返回</p>
<p>语法：同 <code>array.forEach()</code> 方法</p>
<p>例：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">/* 例1: 将大于3的元素，作为一个新数组返回 */</span></span><br><span class="line"><span class="keyword">var</span> newArr = [<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>, <span class="number">4</span>, <span class="number">5</span>].filter(<span class="function"><span class="keyword">function</span> (<span class="params">elem</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">return</span> (elem &gt; <span class="number">3</span>);</span><br><span class="line">&#125;)</span><br><span class="line"><span class="built_in">console</span>.log(newArr) <span class="comment">// [4, 5]</span></span><br><span class="line"><span class="comment">/* 例2: 将所有奇数元素，作为一个新数组返回 */</span></span><br><span class="line"><span class="keyword">var</span> newArr = [<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>, <span class="number">4</span>, <span class="number">5</span>].filter(<span class="function"><span class="keyword">function</span> (<span class="params">elem, index, arr</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">return</span> index % <span class="number">2</span> === <span class="number">0</span>;</span><br><span class="line">&#125;);</span><br><span class="line"><span class="built_in">console</span>.log(newArr) <span class="comment">// [1, 3, 5]</span></span><br></pre></td></tr></table></figure>

<blockquote>
<p>PS：<code>filter()</code> 方法用于过滤数组中的元素</p>
</blockquote>
<h3 id="array-some-与-array-every"><a href="#array-some-与-array-every" class="headerlink" title="array.some() 与 array.every()"></a>array.some() 与 array.every()</h3><p>描述：这两个方法用于判断数组元素是否符合某种条件。返回一个布尔值</p>
<p>语法：同 <code>array.forEach()</code> 方法</p>
<p>例：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">/* 例1 */</span></span><br><span class="line"><span class="keyword">var</span> arr = [<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>, <span class="number">4</span>, <span class="number">5</span>];</span><br><span class="line"><span class="keyword">var</span> value = arr.some(<span class="function"><span class="keyword">function</span> (<span class="params">elem</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">return</span> elem &gt;= <span class="number">3</span>;</span><br><span class="line">&#125;);</span><br><span class="line"><span class="built_in">console</span>.log(value) <span class="comment">// true</span></span><br><span class="line"><span class="comment">/* 例2 */</span></span><br><span class="line"><span class="keyword">var</span> arr = [<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>, <span class="number">4</span>, <span class="number">5</span>];</span><br><span class="line"><span class="keyword">var</span> value = arr.every(<span class="function"><span class="keyword">function</span> (<span class="params">elem, index, arr</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">return</span> elem &gt;= <span class="number">3</span>;</span><br><span class="line">&#125;);</span><br><span class="line"><span class="built_in">console</span>.log(value) <span class="comment">// true</span></span><br></pre></td></tr></table></figure>

<ul>
<li><p><code>some()</code>方法是只要有一个元素的返回值是<code>true</code>，则<code>some()</code>方法的返回值就是<code>true</code>，否则返回<code>false</code></p>
</li>
<li><p><code>every()</code>方法是所有元素的返回值都是<code>true</code>，<code>every()</code>方法才返回<code>true</code>，否则返回<code>false</code></p>
</li>
</ul>
<h3 id="array-reduce-与-array-reduceRight"><a href="#array-reduce-与-array-reduceRight" class="headerlink" title="array.reduce() 与 array.reduceRight()"></a>array.reduce() 与 array.reduceRight()</h3><p>描述：按顺序为数组中的每个元素调用一次指定的函数，每一次执行 reduce方法都会将上一次计算结果( 函数返回值 )作为参数传入，最后将其结果汇总为单个返回值</p>
<p>语法：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">数组.reduce(回调函数, 回调函数中上一次返回值的初始值) <span class="comment">// 从数组第一个元素到数组最后一个元素</span></span><br><span class="line">数组.reduceRight(回调函数, 回调函数中上一次元素值的初始值) <span class="comment">// 从数组最后一个元素到数组第一个元素</span></span><br><span class="line"><span class="comment">//回调函数的语法</span></span><br><span class="line"><span class="function"><span class="keyword">function</span>(<span class="params">上一次返回值, 当前元素值, 当前元素索引, 当前数组</span>)</span>&#123; <span class="keyword">return</span> xxx &#125;</span><br><span class="line"><span class="comment">/*</span></span><br><span class="line"><span class="comment">	上一次返回值：上一次执行回调函数的返回值，默认初始值为数组的第一个元素值</span></span><br><span class="line"><span class="comment">	当前元素值：当前迭代的数组元素值，默认从数组的第二个元素( 索引为1 )开始</span></span><br><span class="line"><span class="comment">	当前元素索引：当前迭代的数组元素索引值</span></span><br><span class="line"><span class="comment">	当前数组：调用reduce方法的数组</span></span><br><span class="line"><span class="comment">*/</span></span><br></pre></td></tr></table></figure>

<p>例：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">/* 例1 */</span></span><br><span class="line"><span class="keyword">var</span> arr = [<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>, <span class="number">4</span>, <span class="number">5</span>];</span><br><span class="line"><span class="keyword">var</span> newArr = arr.reduce(<span class="function"><span class="keyword">function</span> (<span class="params">prev, cur</span>) </span>&#123;</span><br><span class="line">    <span class="built_in">console</span>.log(prev, cur);</span><br><span class="line">	<span class="comment">// 1 2</span></span><br><span class="line">	<span class="comment">// 3 3</span></span><br><span class="line">	<span class="comment">// 6 4</span></span><br><span class="line">	<span class="comment">// 10 5</span></span><br><span class="line">    <span class="keyword">return</span> prev + cur</span><br><span class="line">&#125;)</span><br><span class="line"><span class="built_in">console</span>.log(newArr); <span class="comment">// 15</span></span><br></pre></td></tr></table></figure>

<h2 id="Object-对象"><a href="#Object-对象" class="headerlink" title="Object 对象"></a>Object 对象</h2><h3 id="Object-create"><a href="#Object-create" class="headerlink" title="Object.create()"></a>Object.create()</h3><p>描述：以指定对象为原型创建新的对象，同时可以为新的对象指定新的属性，并对属性进行描述</p>
<p>语法：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="built_in">Object</span>.create(对象, &#123; 属性名: 属性描述符对象... &#125;) <span class="comment">//返回创建的新对象</span></span><br></pre></td></tr></table></figure>

<p>例：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">/* 创建一个干净的对象 */</span></span><br><span class="line"><span class="keyword">var</span> obj = <span class="built_in">Object</span>.create(<span class="literal">null</span>)</span><br><span class="line"><span class="comment">/* 创建一个对象，并添加相应属性，同时对属性进行描述 */</span></span><br><span class="line"><span class="keyword">var</span> obj2 = <span class="built_in">Object</span>.create(<span class="built_in">Object</span>.prototype, &#123;</span><br><span class="line">    <span class="attr">name</span>: &#123;</span><br><span class="line">        <span class="attr">value</span>: <span class="string">&#x27;Timeic&#x27;</span>,</span><br><span class="line">        <span class="attr">writable</span>: <span class="literal">false</span>, <span class="comment">//属性不允许被赋值(=)</span></span><br><span class="line">        <span class="attr">configurable</span>: <span class="literal">false</span>, <span class="comment">//属性不允许从所在对象中删除(delete)</span></span><br><span class="line">        <span class="attr">enumerable</span>: <span class="literal">false</span> <span class="comment">//属性不允许被枚举(for in)</span></span><br><span class="line">    &#125;,</span><br><span class="line">    <span class="attr">age</span>: &#123;</span><br><span class="line">        <span class="attr">value</span>: <span class="number">19</span>,</span><br><span class="line">        <span class="attr">writable</span>: <span class="literal">true</span>, <span class="comment">//属性允许被赋值(=)</span></span><br><span class="line">        <span class="attr">configurable</span>: <span class="literal">true</span>, <span class="comment">//属性允许从所在对象中删除(delete)</span></span><br><span class="line">        <span class="attr">enumerable</span>: <span class="literal">true</span> <span class="comment">//属性允许被枚举(for in)</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;)</span><br><span class="line"><span class="built_in">console</span>.log(obj2);</span><br></pre></td></tr></table></figure>

<h3 id="属性描述符"><a href="#属性描述符" class="headerlink" title="属性描述符"></a>属性描述符</h3><p>属性描述符是 JavaScript 提供的一种<strong>内部数据结构</strong>，用于描述对象的属性值，及控制其行为( 例如该属性是否可写、是否可配置、是否可修改以及是否可枚举等 )</p>
<p>分类：</p>
<ul>
<li>数据描述符</li>
<li>存取描述符</li>
</ul>
<blockquote>
<h3 id="数据描述符（数据属性）"><a href="#数据描述符（数据属性）" class="headerlink" title="数据描述符（数据属性）"></a>数据描述符（数据属性）</h3><table>
<thead>
<tr>
<th>键</th>
<th>值</th>
</tr>
</thead>
<tbody><tr>
<td><code>value</code></td>
<td>该属性对应的值，默认值 undefined</td>
</tr>
<tr>
<td><code>writable</code></td>
<td>值为 <code>true</code> 时，该属性的值( value )才能够被改变，默认值为 false</td>
</tr>
<tr>
<td><code>configurable</code></td>
<td>值为 <code>true</code> 时，该属性描述符的 configurable、enumerable 的值才能修改，同时该属性也能够从所属对象中删除，默认值为 false</td>
</tr>
<tr>
<td><code>enumerable</code></td>
<td>值为 <code>true</code> 时，该属性才会出现在对象的枚举(for in)属性中，默认值为 false</td>
</tr>
</tbody></table>
<h3 id="存取描述符（访问器属性）"><a href="#存取描述符（访问器属性）" class="headerlink" title="存取描述符（访问器属性）"></a>存取描述符（访问器属性）</h3><table>
<thead>
<tr>
<th>键</th>
<th>值</th>
</tr>
</thead>
<tbody><tr>
<td><code>get</code></td>
<td>值为一个函数。没有该函数，则默认值为undefined。当获取或访问当前属性时，会调用此方法</td>
</tr>
<tr>
<td><code>set</code></td>
<td>值为一个函数。没有该函数，则默认值为undefined。当设置当前属性值时，会调用此方法</td>
</tr>
<tr>
<td><code>configurable</code></td>
<td>值为 <code>true</code> 时，该属性描述符的 configurable、enumerable 的值才能修改，同时该属性也能够从所属对象中删除，默认值为 false</td>
</tr>
<tr>
<td><code>enumerable</code></td>
<td>值为 <code>true</code> 时，该属性才会出现在对象的枚举(for in)属性中，默认值为 false</td>
</tr>
</tbody></table>
<h3 id="Object-getOwnPropertyDescriptor"><a href="#Object-getOwnPropertyDescriptor" class="headerlink" title="Object.getOwnPropertyDescriptor()"></a>Object.getOwnPropertyDescriptor()</h3><p>描述：获取属性描述符</p>
<p>语法：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="built_in">Object</span>.getOwnPropertyDescriptor(对象, 属性)</span><br></pre></td></tr></table></figure>

<p>例：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> obj = &#123;&#125;</span><br><span class="line">obj.text = <span class="string">&#x27;string&#x27;</span>;</span><br><span class="line"><span class="built_in">Object</span>.getOwnPropertyDescriptor(obj, <span class="string">&#x27;text&#x27;</span>);</span><br><span class="line"><span class="comment">/* 返回一个属性描述符对象</span></span><br><span class="line"><span class="comment">&#123;</span></span><br><span class="line"><span class="comment">	value: &#x27;string&#x27;,</span></span><br><span class="line"><span class="comment">	writable: true,</span></span><br><span class="line"><span class="comment">	configurable: true,</span></span><br><span class="line"><span class="comment">	enumerable: true</span></span><br><span class="line"><span class="comment">&#125;</span></span><br><span class="line"><span class="comment">*/</span></span><br></pre></td></tr></table></figure>

<h3 id="Object-defineProperty"><a href="#Object-defineProperty" class="headerlink" title="Object.defineProperty()"></a>Object.defineProperty()</h3><p>描述：定义指定对象的属性描述符</p>
<p>语法：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="built_in">Object</span>.defineProperty(对象, 属性, 属性描述符对象)</span><br><span class="line"><span class="comment">/*</span></span><br><span class="line"><span class="comment">第1个参数为目标对象</span></span><br><span class="line"><span class="comment">第2个参数为目标对象的属性名（一个字符串）</span></span><br><span class="line"><span class="comment">第3个参数为属性描述符对象</span></span><br><span class="line"><span class="comment">*/</span></span><br></pre></td></tr></table></figure>

<p>例：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">/* 修改一个值 */</span></span><br><span class="line"><span class="keyword">var</span> obj = &#123;</span><br><span class="line">	<span class="attr">name</span>: <span class="string">&quot;猪八戒&quot;</span></span><br><span class="line">&#125;</span><br><span class="line"><span class="built_in">Object</span>.defineProperty(obj, <span class="string">&quot;name&quot;</span>, &#123; <span class="attr">value</span>: <span class="string">&quot;孙悟空&quot;</span> &#125;);</span><br><span class="line"><span class="built_in">console</span>.log(obj); <span class="comment">// &#123; name: &#x27;孙悟空&#x27; &#125;</span></span><br><span class="line"><span class="comment">/* 新增一个值 */</span></span><br><span class="line"><span class="built_in">Object</span>.defineProperty(obj, <span class="string">&quot;age&quot;</span>, &#123; <span class="attr">value</span>: <span class="number">28</span> &#125;);</span><br><span class="line"><span class="built_in">console</span>.log(obj.age); <span class="comment">// 28</span></span><br></pre></td></tr></table></figure>

<p>PS：使用对象字面量（<code>&#123; 属性名: 属性值 &#125;</code>）或 <code>对象.属性名 = 值</code> 的方式添加的属性 可修改，可删除，可枚举（即属性描述符的<code>enumerable</code>、<code>configurable</code>、<code>writable</code> 的值为 true）</p>
<h3 id="Object-defineProperties"><a href="#Object-defineProperties" class="headerlink" title="Object.defineProperties()"></a>Object.defineProperties()</h3><p>描述：定义指定对象的<strong>多个</strong>属性描述符</p>
<p>语法：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="built_in">Object</span>.defineProperties(对象, &#123; 属性名: 属性描述符对象... &#125;)</span><br></pre></td></tr></table></figure>

<h3 id="getter-amp-setter方法"><a href="#getter-amp-setter方法" class="headerlink" title="getter &amp; setter方法"></a>getter &amp; setter方法</h3><p><code>getter</code> 将对象属性绑定到获取该属性时将被调用的函数</p>
<p>语法：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">&#123;</span><br><span class="line">    get 属性名()&#123;</span><br><span class="line">        <span class="comment">//函数体</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">//get为获取属性，其后为要获取的属性名，当获取该属性时，则调用getter方法</span></span><br></pre></td></tr></table></figure>

<p><code>setter</code> 将对象属性绑定到设置该属性值时将被调用的函数</p>
<p>语法：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">&#123;</span><br><span class="line">    set 属性名(值)&#123;</span><br><span class="line">        <span class="comment">//函数体</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">//set为设置属性，其后为要设置的属性名，当设置该属性值时，则调用setter方法</span></span><br><span class="line"><span class="comment">//setter方法包含一个参数，作为该属性被设置时的新值</span></span><br></pre></td></tr></table></figure>

<p>例：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> obj = &#123;</span><br><span class="line">	<span class="attr">firstName</span>: <span class="string">&#x27;张&#x27;</span>, </span><br><span class="line">	<span class="attr">lastName</span>: <span class="string">&#x27;三&#x27;</span>,</span><br><span class="line">	<span class="keyword">get</span> <span class="title">fullName</span>()&#123;</span><br><span class="line">		<span class="keyword">return</span> <span class="built_in">this</span>.firstName + <span class="string">&#x27; &#x27;</span> + <span class="built_in">this</span>.lastName</span><br><span class="line">	&#125;,</span><br><span class="line">	<span class="keyword">set</span> <span class="title">fullName</span>(<span class="params">data</span>)&#123;</span><br><span class="line">		<span class="keyword">var</span> names = data.split(<span class="string">&#x27; &#x27;</span>);</span><br><span class="line">		<span class="built_in">this</span>.firstName = names[<span class="number">0</span>];</span><br><span class="line">		<span class="built_in">this</span>.lastName = names[<span class="number">1</span>]</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="built_in">console</span>.log(obj);</span><br><span class="line">obj.fullName = <span class="string">&#x27;老 六&#x27;</span>;</span><br><span class="line"><span class="built_in">console</span>.log(obj.fullName);</span><br></pre></td></tr></table></figure>
</blockquote>
<h3 id="Object-getOwnPropertyNames"><a href="#Object-getOwnPropertyNames" class="headerlink" title="Object.getOwnPropertyNames()"></a>Object.getOwnPropertyNames()</h3><p>描述：获取指定对象所有的属性名( 包括不可枚举属性 )集合( 数组 )</p>
<p>语法：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="built_in">Object</span>.keys(对象)</span><br></pre></td></tr></table></figure>

<h3 id="Object-keys"><a href="#Object-keys" class="headerlink" title="Object.keys()"></a>Object.keys()</h3><p>描述：获取指定对象所有可枚举的属性名集合( 数组 )</p>
<p>语法：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="built_in">Object</span>.keys(对象)</span><br></pre></td></tr></table></figure>

<h3 id="Object-values"><a href="#Object-values" class="headerlink" title="Object.values()"></a>Object.values()</h3><p>描述：获取指定对象所有可枚举的属性值集合( 数组 )</p>
<p>语法：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="built_in">Object</span>.values(对象)</span><br></pre></td></tr></table></figure>

<h3 id="Object-entries"><a href="#Object-entries" class="headerlink" title="Object.entries()"></a>Object.entries()</h3><p>描述：获取指定对象所有可枚举属性的名值对集合( 数组 )</p>
<p>语法：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="built_in">Object</span>.entries(对象)</span><br></pre></td></tr></table></figure>

<h3 id="Object-preventExtensions"><a href="#Object-preventExtensions" class="headerlink" title="Object.preventExtensions()"></a>Object.preventExtensions()</h3><p>描述：不可扩展对象（不能再添加新的属性或方法）</p>
<p>例：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> obj = &#123;&#125;;</span><br><span class="line"><span class="built_in">Object</span>.preventExtensions(obj); <span class="comment">//将 obj 设置成不可扩展对象</span></span><br><span class="line">obj.name = <span class="string">&quot;Tom&quot;</span>; <span class="comment">//新增：静默失败</span></span><br><span class="line"><span class="built_in">console</span>.log(obj); <span class="comment">// 空 Object 对象 &#123;&#125;</span></span><br><span class="line"><span class="comment">//使用 Object.defineProperty() 或 Object.defineProperties() 新增属性时会报错</span></span><br><span class="line"><span class="comment">//TypeError: Cannot define property name, object is not extensible</span></span><br><span class="line"><span class="built_in">Object</span>.defineProperty(obj, <span class="string">&quot;name&quot;</span>, &#123;</span><br><span class="line">  <span class="attr">value</span>: <span class="string">&quot;Tom&quot;</span></span><br><span class="line">&#125;);</span><br><span class="line"></span><br><span class="line"><span class="comment">/* 使用 Object.isExtensible() 方法可以判断对象是否可扩展 */</span></span><br><span class="line"><span class="comment">//返回 true表示可扩展 false表示不可扩展</span></span><br><span class="line"><span class="built_in">console</span>.log(<span class="built_in">Object</span>.isExtensible(obj));</span><br></pre></td></tr></table></figure>

<h3 id="Object-seal"><a href="#Object-seal" class="headerlink" title="Object.seal()"></a>Object.seal()</h3><p>描述：密封的对象。一旦将对象进行了密封，则不能为该对象新增属性或方法、同时该对象已有属性的描述符的 <code>configurable</code> 的值将变为 false</p>
<p>例：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> person = &#123;<span class="attr">name</span>: <span class="string">&#x27;Tom&#x27;</span>&#125;;</span><br><span class="line"><span class="built_in">Object</span>.seal(person); <span class="comment">//将 person 设置成密封的对象</span></span><br><span class="line">person.age = <span class="number">12</span>; <span class="comment">//静默失败</span></span><br><span class="line"><span class="comment">//下面修改了已有属性的描述符的 configurable、enumerable 的值</span></span><br><span class="line"><span class="comment">//抛出类型错误 TypeError: Cannot redefine property</span></span><br><span class="line"><span class="built_in">Object</span>.defineProperty(person, <span class="string">&quot;name&quot;</span>, &#123;</span><br><span class="line">  <span class="attr">value</span>: <span class="string">&quot;Bob&quot;</span>,</span><br><span class="line">  <span class="attr">writable</span>: <span class="literal">false</span>,</span><br><span class="line">  <span class="attr">configurable</span>: <span class="literal">true</span>, <span class="comment">//不可修改</span></span><br><span class="line">  <span class="attr">enumerable</span>: <span class="literal">true</span> <span class="comment">//不可修改</span></span><br><span class="line">&#125;);</span><br><span class="line"></span><br><span class="line"><span class="comment">/* 使用 Object.isSealed() 方法可以判断对象是否被密封 */</span></span><br><span class="line"><span class="comment">//返回 true 表示被密封了，false 表示没有被密封</span></span><br><span class="line"><span class="built_in">console</span>.log(<span class="built_in">Object</span>.isExtensible(person));</span><br></pre></td></tr></table></figure>

<h3 id="Object-freeze"><a href="#Object-freeze" class="headerlink" title="Object.freeze()"></a>Object.freeze()</h3><p>描述：冻结的对象。一旦将对象进行了冻结，那么该对象不能做任何修改（不可新增属性或方法，已有属性的描述符的 <code>configurable</code>、<code>writable</code> 的值将变为 false）</p>
<p>例：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> obj = &#123;</span><br><span class="line">  <span class="attr">name</span>: <span class="string">&quot;Tom&quot;</span>,</span><br><span class="line">&#125;;</span><br><span class="line"><span class="built_in">Object</span>.freeze(obj); <span class="comment">//将 obj 设置成冻结的对象</span></span><br><span class="line">obj.age = <span class="number">18</span>; <span class="comment">//新增：静默失败</span></span><br><span class="line">obj.name = <span class="string">&quot;Bob&quot;</span>; <span class="comment">//修改：静默失败</span></span><br><span class="line"><span class="keyword">delete</span> obj.name; <span class="comment">//删除：静默失败</span></span><br><span class="line"><span class="built_in">console</span>.log(obj); <span class="comment">//&#123; name: &#x27;Tom&#x27; &#125;</span></span><br><span class="line"><span class="comment">//使用 Object.defineProperty() 或 Object.defineProperties() 新增属性或修改已属性时都会报错</span></span><br><span class="line"><span class="comment">//TypeError: Cannot redefine property</span></span><br><span class="line"><span class="built_in">Object</span>.defineProperty(obj, <span class="string">&quot;name&quot;</span>, &#123;</span><br><span class="line">  <span class="attr">value</span>: <span class="string">&quot;Bob&quot;</span></span><br><span class="line">&#125;);</span><br><span class="line"><span class="comment">//TypeError: Cannot define property age, object is not extensible</span></span><br><span class="line"><span class="built_in">Object</span>.defineProperty(obj, <span class="string">&quot;age&quot;</span>, &#123;</span><br><span class="line">  <span class="attr">value</span>: <span class="number">18</span></span><br><span class="line">&#125;);</span><br><span class="line"></span><br><span class="line"><span class="comment">/* 使用 Object.isFrozen() 方法可以判断对象是否被冻结 */</span></span><br><span class="line"><span class="comment">//返回 true 表示被冻结了，false 表示没有被冻结</span></span><br><span class="line"><span class="built_in">console</span>.log(<span class="built_in">Object</span>.isExtensible(person))</span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>Web开发</category>
      </categories>
      <tags>
        <tag>JavaScript</tag>
      </tags>
  </entry>
  <entry>
    <title>JavaScript(六) 函数</title>
    <url>/posts/e8048261/</url>
    <content><![CDATA[<link rel="stylesheet" class="aplayer-secondary-style-marker" href="/assets/css/APlayer.min.css"><script src="/assets/js/APlayer.min.js" class="aplayer-secondary-script-marker"></script><p>一个函数是可以通过外部代码<strong>调用</strong>的一个“子程序”。函数中可以封装一系列语句，在需要时直接调用该函数。在定义函数时可以将值传递至函数，同时在调用函数时也可以返回一个值</p>
<h2 id="创建与调用函数"><a href="#创建与调用函数" class="headerlink" title="创建与调用函数"></a>创建与调用函数</h2><p><strong>1、使用 函数表达式 来创建一个函数</strong></p>
<p>语法：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">/* 创建函数 */</span></span><br><span class="line"><span class="keyword">var</span> 变量名 = <span class="function"><span class="keyword">function</span> 函数名(<span class="params">形参<span class="number">1</span>, 形参<span class="number">2.</span>..形参N</span>)</span>&#123;</span><br><span class="line">  <span class="comment">//函数体</span></span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">/* 调用函数 */</span></span><br><span class="line">变量名(实参<span class="number">1</span>, 实参<span class="number">2.</span>..实参N);</span><br></pre></td></tr></table></figure>

<p>例：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">/* 创建函数 */</span></span><br><span class="line"><span class="keyword">var</span> fun = <span class="function"><span class="keyword">function</span>(<span class="params">a, b</span>)</span>&#123;</span><br><span class="line">  <span class="built_in">console</span>.log(a+b)</span><br><span class="line">&#125;</span><br><span class="line"><span class="built_in">console</span>.log(fun.name) <span class="comment">// fun</span></span><br><span class="line"><span class="comment">/* 调用函数 */</span></span><br><span class="line">fun(<span class="number">4</span>, <span class="number">3</span>)</span><br></pre></td></tr></table></figure>

<p><strong>2、使用 函数声明语句 来创建一个函数</strong></p>
<p>语法：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">/* 创建函数 */</span></span><br><span class="line"><span class="function"><span class="keyword">function</span> 函数名(<span class="params">形参<span class="number">1</span>, 形参<span class="number">2.</span>..形参N</span>)</span>&#123;</span><br><span class="line">  <span class="comment">//函数体</span></span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">/* 调用函数 */</span></span><br><span class="line">函数名(实参<span class="number">1</span>, 实参<span class="number">2.</span>..实参N);</span><br></pre></td></tr></table></figure>

<p>例：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">/* 创建函数 */</span></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">fun</span>(<span class="params">arg</span>)</span>&#123;</span><br><span class="line">  <span class="built_in">console</span>.log(<span class="string">&quot;你调用了&quot;</span> + arg + <span class="string">&quot;函数&quot;</span>);</span><br><span class="line">  <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br><span class="line"><span class="built_in">console</span>.log(fun.name) <span class="comment">// fun</span></span><br><span class="line"><span class="comment">/* 调用函数 */</span></span><br><span class="line">fun(<span class="string">&quot;fun&quot;</span>);</span><br></pre></td></tr></table></figure>

<blockquote>
<h3 id="函数体"><a href="#函数体" class="headerlink" title="函数体"></a>函数体</h3><p>函数体为可执行的语句，只在调用函数时执行</p>
<h3 id="形参-与-实参"><a href="#形参-与-实参" class="headerlink" title="形参 与 实参"></a><span title="形式参数">形参</span> 与 <span title="实际参数">实参</span></h3><p>形参是一个未声明的局部变量，只在调用函数时被声明和赋值</p>
<p>而实参则是对应形参的实际值，可以是任何表达式</p>
<p>形参和实参是可选项可以为空</p>
<h3 id="函数表达式"><a href="#函数表达式" class="headerlink" title="函数表达式"></a>函数表达式</h3><p>函数表达式会将该函数( Function类型对象 )作为值返回</p>
<p>在 JavaScript 中 <strong>函数表达式</strong> 和 <strong>函数声明语句</strong> 都使用了 function关键字</p>
<p>Js 引擎在解析一条语句时，如果 function关键字前有相关运算符，则会将其作为函数表达式执行，如果没有则会将其作为函数声明语句执行</p>
<h3 id="函数也是对象"><a href="#函数也是对象" class="headerlink" title="函数也是对象"></a>函数也是对象</h3><p>在 JavaScript 中函数是一个”Function类型对象”，因此函数和对象的使用是一样的</p>
<p>不同的是函数除了可以添加属性外，还可以<strong>封装</strong>和<strong>调用</strong></p>
<h3 id="return"><a href="#return" class="headerlink" title="return"></a>return</h3><p><strong><code>return</code>语句</strong>用来设置函数的返回值</p>
<p>语法：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">return</span> 表达式</span><br></pre></td></tr></table></figure>

<p>例：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">/* 例1 */</span></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">sum</span>(<span class="params">a, b, c</span>)</span>&#123;</span><br><span class="line">	<span class="keyword">return</span> a+b+c</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">var</span> result = sum(<span class="number">4</span>,<span class="number">7</span>,<span class="number">8</span>); <span class="comment">//result 的值为19</span></span><br><span class="line"><span class="comment">/* 例2 */</span></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">fun</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">	<span class="keyword">return</span> &#123;</span><br><span class="line">		<span class="attr">sey</span>: <span class="function"><span class="keyword">function</span> (<span class="params"></span>) </span>&#123;</span><br><span class="line">			<span class="keyword">return</span> <span class="number">123</span>;</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">var</span> s = fun()</span><br><span class="line"><span class="built_in">console</span>.log(s.sey());</span><br></pre></td></tr></table></figure>

<p>return 语句可以终止函数的执行( return 后的语句将不会执行 )，并返回一个指定的值给<strong>函数调用表达式</strong>( 默认返回 undefined )</p>
<p>函数的返回值可以是任何数据类型</p>
</blockquote>
<h2 id="方法（methods）"><a href="#方法（methods）" class="headerlink" title="方法（methods）"></a>方法（<em>methods</em>）</h2><p>在对象中如果一个属性值是一个函数，则该属性可以称为该对象的方法</p>
<p>语法：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">/* 1.通过对象字面量添加方法 */</span></span><br><span class="line"><span class="keyword">var</span> 对象名 = &#123;</span><br><span class="line">  属性名: 函数表达式</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/* 2.通过访问对象表达式添加方法 */</span></span><br><span class="line"><span class="keyword">var</span> 对象名 = &#123;&#125; <span class="comment">//初始化对象</span></span><br><span class="line">对象名.属性名 = 函数表达式</span><br><span class="line"></span><br><span class="line"><span class="comment">/* 方法调用 */</span></span><br><span class="line">对象名.属性名()</span><br></pre></td></tr></table></figure>

<p>例：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">/* 创建对象 */</span></span><br><span class="line"><span class="keyword">var</span> person = &#123;</span><br><span class="line">  <span class="attr">name</span>: <span class="string">&#x27;小明&#x27;</span>,</span><br><span class="line">  <span class="attr">birth</span>: <span class="number">2002</span>,</span><br><span class="line">  <span class="attr">age</span>: <span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">        <span class="keyword">var</span> y = <span class="keyword">new</span> <span class="built_in">Date</span>().getFullYear(); <span class="comment">//返回当前年份到变量y</span></span><br><span class="line">        <span class="keyword">return</span> y - <span class="built_in">this</span>.birth; <span class="comment">//计算年龄并返回给方法调用表达式</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">/* 方法调用 */</span></span><br><span class="line">person.age()</span><br></pre></td></tr></table></figure>

<blockquote>
<h3 id="toString-方法"><a href="#toString-方法" class="headerlink" title="toString 方法"></a>toString 方法</h3><p><code>toString</code> 方法是 Object 类型对象中的一个方法，任何对象都可以使用该方法。该方法用来将任意数据类型转换成字符串并返回</p>
<p>在使用 <code>console.log()</code> 输出对象到控制台时其输出值在不同<strong>运行环境</strong>( 如Chrome、FireFox、Node.js … )会略有不同，默认情况下输出一个对象的值，实际上是输出对象中 <code>toString</code> 方法的<strong>返回值</strong></p>
<p>例：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> obj = &#123;<span class="attr">name</span>: <span class="string">&#x27;value&#x27;</span>&#125;;</span><br><span class="line"><span class="keyword">var</span> arr = [<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>];</span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">fun</span>(<span class="params"></span>)</span>&#123;&#125;</span><br><span class="line"><span class="built_in">console</span>.log(obj.toString()); <span class="comment">// [object Object]</span></span><br><span class="line"><span class="built_in">console</span>.log(arr.toString()); <span class="comment">// 1,2,3</span></span><br><span class="line"><span class="built_in">console</span>.log(fun.toString()); <span class="comment">// function fun()&#123;&#125;</span></span><br><span class="line"><span class="built_in">Object</span>.prototype.toString = <span class="function"><span class="keyword">function</span> (<span class="params"></span>) </span>&#123;</span><br><span class="line"> <span class="keyword">return</span> <span class="string">&#x27;is Object&#x27;</span>;</span><br><span class="line">&#125;</span><br><span class="line">alert(obj); <span class="comment">// is Object</span></span><br></pre></td></tr></table></figure>

<p>当两个对象做比较运算( 如 <code>&lt;</code> <code>&gt;</code> <code>==</code> 仅相等运算有意义 )时，则是比较对象的引用值( 内存地址 )，其它与对象有关的运算，默认情况下都是和对象的 <code>toString</code> 方法的返回值做运算</p>
</blockquote>
<h2 id="立即调用函数表达式（IIFE）"><a href="#立即调用函数表达式（IIFE）" class="headerlink" title="立即调用函数表达式（IIFE）"></a>立即调用函数表达式（<em>IIFE</em>）</h2><p><strong>立即调用函数表达式</strong> 是一个在定义时就会立即执行的函数，也称立即执行函数</p>
<p>它由 <code>函数表达式+()</code> 组成</p>
<p>语法：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">(<span class="function"><span class="keyword">function</span>(<span class="params">[形参<span class="number">1</span>, 形参<span class="number">2.</span>..形参N]</span>)</span>&#123;</span><br><span class="line">  <span class="comment">//函数体</span></span><br><span class="line">&#125;)([实参<span class="number">1</span>, 实参<span class="number">2.</span>..实参N])</span><br></pre></td></tr></table></figure>

<p>例：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> str=<span class="string">&#x27;Hello&#x27;</span></span><br><span class="line">(<span class="function"><span class="keyword">function</span>(<span class="params">a</span>) </span>&#123;</span><br><span class="line">  <span class="built_in">console</span>.log(a);</span><br><span class="line">&#125;)(str)</span><br></pre></td></tr></table></figure>

<h2 id="arguments-对象"><a href="#arguments-对象" class="headerlink" title="arguments 对象"></a>arguments 对象</h2><p><strong><code>arguments</code></strong> 是 Js 引擎在执行时自动向函数中添加的<strong>局部变量</strong>，其值是一个保存了函数实参的<strong>类数组对象</strong>（类似数组，但不是数组）</p>
<p>属性：</p>
<ul>
<li><p><strong><code>.length</code> 获取实参的个数</strong></p>
</li>
<li><p><strong><code>.callee</code> 返回当前执行的函数</strong></p>
</li>
<li><p><strong><code>[下标]</code> 根据索引返回或修改实参的值</strong></p>
</li>
</ul>
<p>例：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">/* 例1 */</span></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">fun</span>(<span class="params"></span>)</span>&#123;</span><br><span class="line">  <span class="built_in">console</span>.log(<span class="built_in">arguments</span>.length)</span><br><span class="line">&#125;</span><br><span class="line">fun(<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>, <span class="number">4</span>) <span class="comment">//执行结果：4</span></span><br><span class="line"><span class="comment">/* 例2 */</span></span><br><span class="line">(<span class="function"><span class="keyword">function</span> (<span class="params"></span>)</span>&#123;</span><br><span class="line">  <span class="built_in">arguments</span>[<span class="number">0</span>] = <span class="number">0</span>;</span><br><span class="line">  <span class="built_in">console</span>.log(<span class="built_in">arguments</span>[<span class="number">0</span>], <span class="built_in">arguments</span>[<span class="number">1</span>], <span class="built_in">arguments</span>[<span class="number">2</span>]); <span class="comment">//输出：0 2 3</span></span><br><span class="line">&#125;)(<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>)</span><br></pre></td></tr></table></figure>

<h2 id="全局与局部变量"><a href="#全局与局部变量" class="headerlink" title="全局与局部变量"></a>全局与局部变量</h2><h3 id="局部变量"><a href="#局部变量" class="headerlink" title="局部变量"></a>局部变量</h3><p>通常函数内声明的变量都为局部变量，在调用函数时函数内声明的变量只在该函数内有效</p>
<p>例：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">(<span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">  <span class="keyword">var</span> lv=<span class="string">&#x27;我是局部变量&#x27;</span>;</span><br><span class="line">  <span class="comment">/* 在函数内输出 */</span></span><br><span class="line">  <span class="built_in">console</span>.log(lv); <span class="comment">// 我是局部变量</span></span><br><span class="line">&#125;)()</span><br><span class="line"><span class="comment">/* 在函数外输出 */</span></span><br><span class="line"><span class="built_in">console</span>.log(lv) <span class="comment">//报错：test is not defined</span></span><br></pre></td></tr></table></figure>

<h3 id="全局变量"><a href="#全局变量" class="headerlink" title="全局变量"></a>全局变量</h3><p>默认情况下，不再函数内声明的变量都为全局变量，如果变量不声明(<del><code>var</code></del>)直接赋值，则该变量直接为全局变量</p>
<p>全局变量在函数内或函数外都可以访问</p>
<p>例：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">/* 例1 */</span></span><br><span class="line"><span class="keyword">var</span> gv = <span class="string">&#x27;我是全局变量&#x27;</span>;</span><br><span class="line">(<span class="function"><span class="keyword">function</span>(<span class="params"></span>)</span>&#123;</span><br><span class="line">  <span class="comment">/* 在函数内输出 */</span></span><br><span class="line">  <span class="built_in">console</span>.log(gv); <span class="comment">// 我是全局变量</span></span><br><span class="line">&#125;())</span><br><span class="line"><span class="comment">/* 在函数外输出 */</span></span><br><span class="line"><span class="built_in">console</span>.log(gv) <span class="comment">// 我是全局变量</span></span><br><span class="line"></span><br><span class="line"><span class="comment">/* 例2 */</span></span><br><span class="line">(<span class="function"><span class="keyword">function</span>(<span class="params"></span>)</span>&#123;</span><br><span class="line">	a = <span class="number">666</span>;</span><br><span class="line">&#125;)()</span><br><span class="line"><span class="built_in">console</span>.log(a, <span class="built_in">window</span>.a) <span class="comment">// 666 666</span></span><br></pre></td></tr></table></figure>

<blockquote>
<h3 id="window-对象"><a href="#window-对象" class="headerlink" title="window 对象"></a>window 对象</h3><p>默认情况下，全局变量会作为在全局对象<code>window</code>的属性和方法存储</p>
<p>在 Js 中，访问一个全局变量，默认会从全局对象<code>window</code>中访问</p>
<p>例：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> a = <span class="number">123</span>;</span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">fun</span>(<span class="params"></span>)</span>&#123;</span><br><span class="line">	<span class="keyword">return</span> a;</span><br><span class="line">&#125;</span><br><span class="line"><span class="built_in">console</span>.log(<span class="built_in">window</span>.a, <span class="built_in">window</span>.fun()); <span class="comment">//输出：123 123</span></span><br></pre></td></tr></table></figure>
</blockquote>
<h2 id="作用域（scope）"><a href="#作用域（scope）" class="headerlink" title="作用域（scope）"></a>作用域（<em>scope</em>）</h2><p>作用域是指一个变量作用的 <strong>范围</strong></p>
<p>分类：</p>
<ul>
<li>全局作用域</li>
<li>函数作用域</li>
</ul>
<p>全局作用域中的变量即全局变量，函数作用域中的变量即局部变量</p>
<p>在浏览器中，全局作用域其作用范围是整个页面。函数作用域其作用范围只在函数内</p>
<h3 id="作用域链"><a href="#作用域链" class="headerlink" title="作用域链"></a>作用域链</h3><p>函数内可以嵌套多个函数，而作用域则是根据函数代码的层次进行分层，以使子作用域可以访问父级作用域中的变量，而不能从父作用域引用子级作用域中的变量</p>
<p>如果一个变量不 “在当前的作用域中”，则会从父级作用域中查找，即沿着链式的作用域链查找( 就近原则 )</p>
<p>例：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> num = <span class="number">10</span>;</span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">fun1</span>(<span class="params"></span>) </span>&#123;	<span class="comment">//外层函数</span></span><br><span class="line">	<span class="keyword">var</span> num = <span class="number">20</span>;</span><br><span class="line">	<span class="function"><span class="keyword">function</span> <span class="title">fun2</span>(<span class="params"></span>) </span>&#123;	<span class="comment">//内层函数</span></span><br><span class="line">		<span class="built_in">console</span>.log(num); <span class="comment">//输出结果: 20</span></span><br><span class="line">	&#125;</span><br><span class="line">	fun2();</span><br><span class="line">&#125;</span><br><span class="line">fun1();</span><br></pre></td></tr></table></figure>

<blockquote>
<p><img src="https://cdn3.tianli0.top/gh/k34869/images/image/20221112103929" alt="作用域"></p>
<p>当内层函数要访问一个变量时，会先在当前作用域中查找，若不存在，则向上一级作用域中查找，直到找到全局作用域，如果全局作用域中依然没有找到，则会报错 ReferenceError</p>
</blockquote>
<h3 id="变量提升（Hoisting）"><a href="#变量提升（Hoisting）" class="headerlink" title="变量提升（Hoisting）"></a>变量提升（<em>Hoisting</em>）</h3><p><strong>使用var关键字声明，会在当前作用域中提前声明</strong></p>
<p>例：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="built_in">console</span>.log(a);	<span class="comment">//输出：undefined</span></span><br><span class="line"><span class="built_in">console</span>.log(b);	<span class="comment">//报错：b is not defined</span></span><br><span class="line"><span class="keyword">var</span> a = <span class="number">1</span>;			<span class="comment">//这里发生变量提升</span></span><br><span class="line">b = <span class="number">2</span>;					<span class="comment">//不使用var关键字声明，则不会发生变量提升</span></span><br><span class="line"><span class="built_in">console</span>.log(a, b); <span class="comment">//输出：1 2</span></span><br></pre></td></tr></table></figure>

<p><strong>使用function关键字声明，会在当前作用域中提前声明</strong></p>
<p>例：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">fn();</span><br><span class="line">fun(); <span class="comment">//错误：fun is not a function</span></span><br><span class="line"><span class="built_in">console</span>.log(fun); <span class="comment">//输出：undefined</span></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">fn</span>(<span class="params"></span>) </span>&#123; <span class="comment">//function声明会将引用值一起提升</span></span><br><span class="line">  <span class="built_in">console</span>.log(<span class="string">&quot;this is function&quot;</span>);</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">var</span> fun = <span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123; <span class="comment">//这里只提升声明</span></span><br><span class="line">  <span class="built_in">console</span>.log(<span class="string">&quot;this is function&quot;</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><strong><code>function</code>声明要比<code>var</code>声明的优先级更高</strong></p>
<p>例：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="built_in">console</span>.log(fn.toString()); <span class="comment">// [Function: fn]</span></span><br><span class="line"><span class="keyword">var</span> fn = <span class="number">100</span>;</span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">fn</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">	<span class="built_in">console</span>.log(<span class="string">&quot;this is function&quot;</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<blockquote>
<p>PS：JavaScript 中 var 关键字只会提升声明，不会提升其初始化的值，而 function 声明则会将其引用值提升</p>
</blockquote>
]]></content>
      <categories>
        <category>Web开发</category>
      </categories>
      <tags>
        <tag>JavaScript</tag>
      </tags>
  </entry>
  <entry>
    <title>JavaScript(四) 流程控制</title>
    <url>/posts/fa1154a/</url>
    <content><![CDATA[<link rel="stylesheet" class="aplayer-secondary-style-marker" href="/assets/css/APlayer.min.css"><script src="/assets/js/APlayer.min.js" class="aplayer-secondary-script-marker"></script><p>js 中默认是从上到下一行一行执行的，这种按照顺序执行在程序中就被称作顺序结构，流程控制就是指控制程序代码的执行顺序</p>
<p>三种基本程序结构：</p>
<p><img src="https://cdn3.tianli0.top/gh/k34869/images/image/20221112102610" alt="流程控制"></p>
<h2 id="顺序结构"><a href="#顺序结构" class="headerlink" title="顺序结构"></a>顺序结构</h2><p>顺序结构是程序中最简单、最基本的程序结构，它没有特定的语法，程序会按照代码的先后顺序，依次执行，程序中大多数的代码都是这样执行的</p>
<h2 id="分支结构"><a href="#分支结构" class="headerlink" title="分支结构"></a>分支结构</h2><p>分支结构在执行代码的时候，可以根据条件进行选择，条件越多对应的结果越多，分支也就越多。在 Js 中通过 <span title="如果...否则..."><code>if...else...</code></span> 语句、<span title="切换、开关"><code>switch</code></span> 语句 来实现分支结构</p>
<h3 id="单路分支"><a href="#单路分支" class="headerlink" title="单路分支"></a>单路分支</h3><p>语法：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">if</span>(条件)</span><br><span class="line">  <span class="comment">//条件为 true 时执行此处语句;</span></span><br></pre></td></tr></table></figure>

<p>例：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> a = prompt(<span class="string">&quot;请输入...&quot;</span>,<span class="string">&quot;&quot;</span>);</span><br><span class="line"><span class="keyword">if</span>(a &gt; <span class="number">4</span>)</span><br><span class="line">  alert(<span class="string">&quot;输入的数字大于4&quot;</span>);</span><br></pre></td></tr></table></figure>

<blockquote>
<h3 id="块语句"><a href="#块语句" class="headerlink" title="块语句"></a>块语句</h3><p>如果 <strong>if(条件)</strong> 后的语句有多个( 默认只匹配第一条语句 )，需要使用<code>&#123;&#125;</code>来对代码进行分组，写在<code>&#123;&#125;</code>中的代码会作为一个整体一起执行，如果只有一条语句可以省略大括号<code>&#123;&#125;</code></p>
<p>语法：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">&#123;</span><br><span class="line">	<span class="comment">//一组语句</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</blockquote>
<h3 id="双路分支"><a href="#双路分支" class="headerlink" title="双路分支"></a>双路分支</h3><p>语法：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">if</span> (条件) &#123;</span><br><span class="line">    <span class="comment">//条件为 true 时执行此处语句</span></span><br><span class="line">&#125; <span class="keyword">else</span> &#123;</span><br><span class="line">    <span class="comment">//条件为 false 时执行此处语句</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>例：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> pwd = prompt(<span class="string">&quot;请输入密码&quot;</span>,<span class="string">&quot;&quot;</span>);</span><br><span class="line"><span class="keyword">if</span> (pwd == <span class="number">123</span>) &#123;</span><br><span class="line">    alert(<span class="string">&quot;密码正确&quot;</span>);</span><br><span class="line">&#125; <span class="keyword">else</span> &#123;</span><br><span class="line">    alert(<span class="string">&quot;密码错误&quot;</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<blockquote>
<p>PS：if…else…语句可以根据 <strong>条件</strong> 来选择执行，而条件是一个返回值为<code>true</code>或<code>false</code>的表达式</p>
</blockquote>
<h3 id="多路分支"><a href="#多路分支" class="headerlink" title="多路分支"></a>多路分支</h3><p><code>if..else...</code>语句中要实现多路分支，可以使用<code>else if</code>从句</p>
<p>语法：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">if</span> (条件<span class="number">1</span>) &#123;</span><br><span class="line">    <span class="comment">//条件1为 true 时执行此处语句</span></span><br><span class="line">&#125; <span class="keyword">else</span> <span class="keyword">if</span> (条件<span class="number">2</span>) &#123;</span><br><span class="line">    <span class="comment">//条件2为 true 时执行此处语句</span></span><br><span class="line">&#125; <span class="keyword">else</span> <span class="keyword">if</span> (条件<span class="number">3</span>) &#123;</span><br><span class="line">    <span class="comment">//条件3为 true 时执行此处语句</span></span><br><span class="line">&#125; ... <span class="keyword">else</span> &#123;</span><br><span class="line">    <span class="comment">//上述所有条件都为 false 时执行此处语句</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>例：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> score = prompt(<span class="string">&quot;请输入你的成绩&quot;</span>,<span class="string">&quot;&quot;</span>);</span><br><span class="line"><span class="keyword">if</span> (score &lt; <span class="number">60</span>) &#123;</span><br><span class="line">    alert(<span class="string">&quot;不及格&quot;</span>);</span><br><span class="line">&#125; <span class="keyword">else</span> <span class="keyword">if</span> (score &gt;= <span class="number">60</span> &amp;&amp; score &lt;= <span class="number">70</span>) &#123;</span><br><span class="line">    alert(<span class="string">&quot;及格&quot;</span>);</span><br><span class="line">&#125; <span class="keyword">else</span> <span class="keyword">if</span> (score &gt; <span class="number">70</span> &amp;&amp; score &lt;= <span class="number">80</span>) &#123;</span><br><span class="line">    alert(<span class="string">&quot;良好&quot;</span>);</span><br><span class="line">&#125; <span class="keyword">else</span> <span class="keyword">if</span> (score &gt; <span class="number">80</span> &amp;&amp; score &lt;= <span class="number">90</span>) &#123;</span><br><span class="line">    alert(<span class="string">&quot;优秀&quot;</span>);</span><br><span class="line">&#125; <span class="keyword">else</span> &#123;</span><br><span class="line">    alert(<span class="string">&quot;人才&quot;</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<blockquote>
<p>PS：在多路分支中当执行到某个分支且该分支的条件成立时，则执行该分支中的语句，执行完成后会自动从所有分支中跳出，如果该分支不成立则继续判断下一分支( <code>else if</code> )是否成立，以此类推…</p>
</blockquote>
<h3 id="switch语句"><a href="#switch语句" class="headerlink" title="switch语句"></a>switch语句</h3><p>在 Js 中还可以使用switch语句来实现分支结构</p>
<p>语法1：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">switch</span>(表达式)&#123;</span><br><span class="line">  <span class="keyword">case</span> 值<span class="number">1</span>:</span><br><span class="line">    <span class="comment">//当 &quot;表达式&quot; 与 &quot;值1&quot; 全等时，执行此处语句</span></span><br><span class="line">    <span class="keyword">break</span>;</span><br><span class="line">  <span class="keyword">case</span> 值<span class="number">2</span>:</span><br><span class="line">    <span class="comment">//当 &quot;表达式&quot; 与 &quot;值2&quot; 全等时，执行此处语句</span></span><br><span class="line">    <span class="keyword">break</span>;</span><br><span class="line">  <span class="keyword">case</span> 值<span class="number">3</span>:</span><br><span class="line">    <span class="comment">//当 &quot;表达式&quot; 与 &quot;值3&quot; 全等时，执行此处语句</span></span><br><span class="line">    <span class="keyword">break</span>;</span><br><span class="line">  ......</span><br><span class="line">  <span class="attr">default</span>:</span><br><span class="line">    <span class="comment">//当上述都不成立时执行的语句</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>例：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> num = prompt(<span class="string">&quot;请输入...&quot;</span>,<span class="string">&quot;&quot;</span>);</span><br><span class="line"><span class="keyword">switch</span>(num) &#123;</span><br><span class="line">  <span class="keyword">case</span> <span class="string">&quot;1&quot;</span>:</span><br><span class="line">    alert(<span class="string">&quot;星期一&quot;</span>);</span><br><span class="line">    <span class="keyword">break</span>;</span><br><span class="line">  <span class="keyword">case</span> <span class="string">&quot;2&quot;</span>:</span><br><span class="line">    alert(<span class="string">&quot;星期二&quot;</span>);</span><br><span class="line">    <span class="keyword">break</span>;</span><br><span class="line">  <span class="keyword">case</span> <span class="string">&quot;3&quot;</span>:</span><br><span class="line">    alert(<span class="string">&quot;星期三&quot;</span>);</span><br><span class="line">    <span class="keyword">break</span>;</span><br><span class="line">  <span class="keyword">case</span> <span class="string">&quot;4&quot;</span>:</span><br><span class="line">    alert(<span class="string">&quot;星期四&quot;</span>);</span><br><span class="line">    <span class="keyword">break</span>;</span><br><span class="line">  <span class="keyword">case</span> <span class="string">&quot;5&quot;</span>:</span><br><span class="line">    alert(<span class="string">&quot;星期五&quot;</span>);</span><br><span class="line">    <span class="keyword">break</span>;</span><br><span class="line">  <span class="keyword">case</span> <span class="string">&quot;6&quot;</span>:</span><br><span class="line">    alert(<span class="string">&quot;星期六&quot;</span>);</span><br><span class="line">    <span class="keyword">break</span>;</span><br><span class="line">  <span class="keyword">case</span> <span class="string">&quot;7&quot;</span>:</span><br><span class="line">    alert(<span class="string">&quot;星期日&quot;</span>);</span><br><span class="line">    <span class="keyword">break</span>;</span><br><span class="line">  <span class="keyword">default</span>:</span><br><span class="line">    alert(<span class="string">&quot;请输入1-7以内的数字&quot;</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<blockquote>
<h3 id="执行流程"><a href="#执行流程" class="headerlink" title="执行流程"></a>执行流程</h3><p>将switch后括号内的 <strong>表达式</strong> 与case后的 <strong>表达式</strong> 逐一进行全等(<code>===</code>)比较</p>
<ul>
<li>当比较结果为 true 时，则停止比较，并从当前 case处向下执行( 不会自动跳出switch语句 )</li>
<li>当比较结果为 false 时，则继续比较下一个 case后的表达式</li>
</ul>
<h3 id="case"><a href="#case" class="headerlink" title="case"></a>case</h3><p>用于匹配<code>switch(表达式)</code>括号中表达式的子句</p>
<p>它会从第一个 case 子句开始匹配，直到匹配成立( 全等, === )，则匹配结束( 不再匹配 )，执行其后的相关语句</p>
<h3 id="break"><a href="#break" class="headerlink" title="break"></a>break</h3><p>break 语句用于跳出 switch语句的执行( 结束switch语句 )</p>
<h3 id="default"><a href="#default" class="headerlink" title="default"></a>default</h3><p>default 后的语句无需比较，直接执行，因此要放到最后</p>
</blockquote>
<h2 id="循环结构"><a href="#循环结构" class="headerlink" title="循环结构"></a>循环结构</h2><p>循环结构就是重复不断的做一件事，在 Js 中通过<code>for</code>语句、<code>while</code>语句、<code>do...while</code>语句 来实现循环结构</p>
<h3 id="for语句"><a href="#for语句" class="headerlink" title="for语句"></a>for语句</h3><p>语法：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">for</span>(初始化; 条件; 更新表达式) &#123;</span><br><span class="line">    <span class="comment">// 循环体</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>执行步骤：</p>
<ol>
<li>执行初始化(初始化变量)，初始化只会执行一次</li>
<li>执行条件，如果条件为 true，则执行循环体，并继续步骤③，如果为 false，跳出循环(循环结束)</li>
<li>执行更新表达式，更新表达式执行完毕重复步骤②</li>
</ol>
<p>例：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">//输出数字1-10</span></span><br><span class="line"><span class="keyword">for</span>(<span class="keyword">var</span> i=<span class="number">0</span>; i&lt;=<span class="number">10</span>; i++) &#123;</span><br><span class="line">  <span class="built_in">console</span>.log(i);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<blockquote>
<h3 id="初始化"><a href="#初始化" class="headerlink" title="初始化"></a>初始化</h3><p>初始化只会在第一次循环执行，通常为一个赋值语句。典型地被用于初始化一个计数器( 初始化条件 )</p>
<h3 id="条件"><a href="#条件" class="headerlink" title="条件"></a>条件</h3><p>条件被用于确定每一次循环是否能被执行。如果条件为 true，则执行循环体。如果条件为 false，则从循环体中跳出( 循环结束 )，如果条件为空，则每次循环条件都为 true，即无限循环</p>
<h3 id="更新表达式"><a href="#更新表达式" class="headerlink" title="更新表达式"></a>更新表达式</h3><p>每次循环体执行完后都会自动跳回更新表达式。更新表达式会在下一次<strong>条件</strong>执行前执行。通常被用于更新或者自增计数器变量( 更新条件 )</p>
</blockquote>
<h3 id="while语句"><a href="#while语句" class="headerlink" title="while语句"></a>while语句</h3><p>语法：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">while</span>(条件) &#123;</span><br><span class="line">  <span class="comment">//循环体</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>例：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> n=<span class="number">0</span>;	<span class="comment">//初始化</span></span><br><span class="line"><span class="keyword">var</span> x=<span class="number">0</span>;</span><br><span class="line"><span class="keyword">while</span>(n &lt; <span class="number">3</span>) &#123;</span><br><span class="line">  n++;		<span class="comment">//更新表达式</span></span><br><span class="line">  <span class="built_in">console</span>.log(x+=n);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<blockquote>
<h3 id="执行流程-1"><a href="#执行流程-1" class="headerlink" title="执行流程"></a>执行流程</h3><p>当条件为 true 时执行循环体，执行完成后则自动跳回条件判断，直到当条件为 false 时则从循环体中跳出( 循环结束 )</p>
<p>按照顺序结构，while语句需要将更新表达式写在循环体中，初始化需要写在whlie语句前面</p>
</blockquote>
<h3 id="do…while语句"><a href="#do…while语句" class="headerlink" title="do…while语句"></a>do…while语句</h3><p>语法：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">do</span> &#123;</span><br><span class="line">   <span class="comment">//循环体</span></span><br><span class="line">&#125; <span class="keyword">while</span>(条件);</span><br></pre></td></tr></table></figure>

<p>例：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">//输出数字1-10</span></span><br><span class="line"><span class="keyword">var</span> i = <span class="number">1</span>;  <span class="comment">//初始化</span></span><br><span class="line"><span class="keyword">do</span>&#123;</span><br><span class="line">	<span class="built_in">console</span>.log(i);</span><br><span class="line">  i++;	<span class="comment">//更新表达式</span></span><br><span class="line">&#125;<span class="keyword">while</span>(i &lt;= <span class="number">10</span>);</span><br></pre></td></tr></table></figure>

<blockquote>
<h3 id="执行流程-2"><a href="#执行流程-2" class="headerlink" title="执行流程"></a>执行流程</h3><p>先执行循环体，循环体执行完后，执行条件，如果条件为 true，则继续执行循环体，如果条件为 false 则停止循环向下执行</p>
</blockquote>
<h2 id="break与continue"><a href="#break与continue" class="headerlink" title="break与continue"></a>break与continue</h2><p><strong>break 语句</strong>可以中止当前循环、<code>switch</code>语句或<code>label</code>语句</p>
<p>语法：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">break</span> [label]</span><br></pre></td></tr></table></figure>

<p>例：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">for</span>(<span class="keyword">var</span> i=<span class="number">0</span>; i&lt;<span class="number">5</span>; i++)&#123;</span><br><span class="line">	<span class="built_in">console</span>.log(i);</span><br><span class="line">	<span class="keyword">break</span>	<span class="comment">//终止当前循环</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><strong>continue 语句</strong>可以终止当前循环或标记循环的当前迭代中的语句，并在下一次迭代时继续执行循环</p>
<p>语法：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">continue</span> [label]</span><br></pre></td></tr></table></figure>

<p>例：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">for</span>(<span class="keyword">var</span> i=<span class="number">0</span>; i&lt;<span class="number">5</span>; i++)&#123;</span><br><span class="line">  <span class="keyword">if</span>(i==<span class="number">2</span>)&#123;</span><br><span class="line">    <span class="keyword">continue</span>;</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="built_in">console</span>.log(i) <span class="comment">//输出0 1 3 4</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<blockquote>
<h3 id="label（标记语句）"><a href="#label（标记语句）" class="headerlink" title="label（标记语句）"></a>label（标记语句）</h3><p><code>break</code> 和 <code>continue</code>语句包含一个可选的标签(label)，可允许程序跳出一个被标记的语句。</p>
<p>语法：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">标签名: 被标记的语句</span><br><span class="line"><span class="comment">//标签名建议遵循标识符规范命名</span></span><br><span class="line"><span class="comment">//被标记的语句可以是一个循环语句、switch语句，也可以直接是一个块语句</span></span><br></pre></td></tr></table></figure>

<p>例1：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">outer: <span class="keyword">for</span>(<span class="keyword">var</span> i=<span class="number">0</span>; i&lt;<span class="number">5</span>; i++)&#123;</span><br><span class="line">	<span class="built_in">console</span>.log(<span class="string">&quot;@外层循环&quot;</span>+i)</span><br><span class="line">	<span class="keyword">for</span>(<span class="keyword">var</span> j=<span class="number">0</span>; j&lt;<span class="number">5</span>; j++)&#123;</span><br><span class="line">		<span class="keyword">if</span>(j==<span class="number">3</span>)&#123;</span><br><span class="line">		<span class="keyword">break</span> outer <span class="comment">//终止外层循环(按照顺序结构，外层循环跳过，内层循环也不会执行)</span></span><br><span class="line">		&#125;</span><br><span class="line">		<span class="built_in">console</span>.log(<span class="string">&quot;@内层循环&quot;</span>+j)</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>例2：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">outer: <span class="keyword">for</span>(<span class="keyword">var</span> i=<span class="number">0</span>; i&lt;<span class="number">5</span>; i++)&#123;</span><br><span class="line"><span class="built_in">console</span>.log(<span class="string">&quot;@外层循环&quot;</span>+i)</span><br><span class="line"><span class="keyword">for</span>(<span class="keyword">var</span> j=<span class="number">0</span>; j&lt;<span class="number">5</span>; j++)&#123;</span><br><span class="line">		<span class="keyword">if</span>(j==<span class="number">1</span>)&#123;</span><br><span class="line">			<span class="keyword">continue</span> outer</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="built_in">console</span>.log(<span class="string">&quot;@内层循环&quot;</span>+j)</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="break和continue的区别"><a href="#break和continue的区别" class="headerlink" title="break和continue的区别"></a>break和continue的区别</h3><p>break语句可以在循环语句( 如for、while等 )、switch语句中使用，而 continue 只能在 循环语句 中使用</p>
<p><code>continue</code> 与 <code>break</code> 语句的区别在于，continue 并不会终止整个循环，而是：</p>
<ul>
<li><p>在 <code>while</code> 循环中，会跳回条件判断</p>
</li>
<li><p>在 <code>for</code> 循环中，会跳回更新表达式</p>
</li>
</ul>
</blockquote>
]]></content>
      <categories>
        <category>Web开发</category>
      </categories>
      <tags>
        <tag>JavaScript</tag>
      </tags>
  </entry>
  <entry>
    <title>DOS命令</title>
    <url>/posts/ba611fe6/</url>
    <content><![CDATA[<link rel="stylesheet" class="aplayer-secondary-style-marker" href="/assets/css/APlayer.min.css"><script src="/assets/js/APlayer.min.js" class="aplayer-secondary-script-marker"></script><p>DOS是<strong>Disk Operating System</strong>(磁盘操作系统)的缩写，常见的DOS有两种：IBM公司的<a href="https://baike.baidu.com/item/PC-DOS">PC-DOS</a>和微软公司的<a href="https://baike.baidu.com/item/MS-DOS">MS-DOS</a></p>
<p><strong>MS-DOS</strong> 它是由美国微软公司发展的操作系统，运行在<strong>Intel x86</strong>个人电脑上。自微软图形界面操作系统<a href="https://baike.baidu.com/item/Windows%20NT">Windows NT</a>问世以来，DOS就作为一个后台程序的形式出现，我们可以通过键盘快捷键<kbd>Win</kbd> + <kbd>R</kbd> 打开运行窗口-在里面输入 <code>cmd</code> 回车即可进入DOS界面。</p>
<p><img src="https://cdn3.tianli0.top/gh/k34869/images/image/20220728132900" alt="cmd"></p>
<blockquote>
<p><em>DOS命令，计算机术语，是指DOS操作系统(单任务的操作系统)的命令，是一种面向磁盘的操作命令，主要包括目录操作类命令、磁盘操作类命令、文件操作类命令和其它命令。</em></p>
</blockquote>
<h2 id="内部命令与外部命令"><a href="#内部命令与外部命令" class="headerlink" title="内部命令与外部命令"></a>内部命令与外部命令</h2><p>内部命令，是指在DOS启动之后，自动通过<code>command.com</code>文件导入内存的命令。在执行这些命令的时候，直接从内存进行调用。<code>command.com</code>文件是DOS的核心程序，也就是说内部命令是由DOS自身提供。而外部命令是一条单独的<strong>可执行文件</strong>。</p>
<p>外部命令需要保证程序的可执行文件(.exe或.com)在当前的目录中，或在环境变量<code>PATH</code>中。</p>
<p>DOS的外部命令就是一些应用程序，这些外部命令都是以文件的形式存在。</p>
<p>内部命令相比于外部命令其特点就是执行速度快且无需额外安装在任何时候都可以使用。</p>
<h2 id="外部命令"><a href="#外部命令" class="headerlink" title="外部命令"></a>外部命令</h2><table><thead><tr><th align="center">命令</th><th align="center">描述</th><th align="center">命令</th><th align="center">描述</th></tr></thead><tbody><tr><td align="center">calc</td><td align="center">启动计算器</td><td align="center">diskmgmt.msc</td><td align="center">磁盘管理实用程序</td></tr><tr><td align="center">certmgr.msc</td><td align="center">证书管理实用程序</td><td align="center">dvdplay</td><td align="center">DVD播放器</td></tr><tr><td align="center">charmap</td><td align="center">启动字符映射表</td><td align="center">dxdiag</td><td align="center">检查DirectX信息</td></tr><tr><td align="center">chkdsk.exe</td><td align="center">Chkdsk磁盘检查</td><td align="center">eudcedit</td><td align="center">造字程序</td></tr><tr><td align="center">cmd.exe</td><td align="center">CMD命令提示符</td><td align="center">eventvwr</td><td align="center">事件查看器</td></tr><tr><td align="center">compmgmt.msc</td><td align="center">打开计算机管理</td><td align="center">explorer</td><td align="center">打开资源管理器</td></tr><tr><td align="center">cliconfg</td><td align="center">SQL SERVER 客户端网络实用程序</td><td align="center">fsmgmt.msc</td><td align="center">共享文件夹管理器</td></tr><tr><td align="center">dcomcnfg</td><td align="center">打开系统组件服务</td><td align="center">gpedit.msc</td><td align="center">组策略</td></tr><tr><td align="center">devmgmt.msc</td><td align="center">设备管理器</td><td align="center">netplwiz</td><td align="center">用户账户</td></tr><tr><td align="center">logoff</td><td align="center">注销命令</td><td align="center">ntmsmgr.msc</td><td align="center">移动存储管理器</td></tr><tr><td align="center">lusrmgr.msc</td><td align="center">本机用户和组</td><td align="center">ntmsoprq.msc</td><td align="center">移动存储管理员操作请求</td></tr><tr><td align="center">magnify</td><td align="center">放大镜实用程序</td><td align="center">odbcad32</td><td align="center">ODBC数据源管理器</td></tr><tr><td align="center">mmc</td><td align="center">打开控制台</td><td align="center">osk</td><td align="center">打开屏幕键盘</td></tr><tr><td align="center">mobsync</td><td align="center">同步命令</td><td align="center">perfmon.msc</td><td align="center">计算机性能监测程序</td></tr><tr><td align="center">Msconfig.exe</td><td align="center">系统配置实用程序</td><td align="center">regedit.exe</td><td align="center">注册表</td></tr><tr><td align="center">mspaint</td><td align="center">画图板</td><td align="center">regedt32</td><td align="center">注册表编辑器</td></tr><tr><td align="center">mstsc</td><td align="center">远程桌面连接</td><td align="center">rononce -p</td><td align="center">15秒关机</td></tr><tr><td align="center">narrator</td><td align="center">屏幕“讲述人”</td><td align="center">regsvr32 /u zipfldr.dll</td><td align="center">取消ZIP支持</td></tr><tr><td align="center">netstat -an</td><td align="center">(TC)命令检查接口</td><td align="center">regsvr32 /u *.dll</td><td align="center">停止dll文件运行</td></tr><tr><td align="center">notepad</td><td align="center">打开记事本</td><td align="center">rsop.msc</td><td align="center">组策略结果集</td></tr><tr><td align="center">Nslookup</td><td align="center">IP地址侦测器</td><td align="center">secpol.msc</td><td align="center">本地安全策略</td></tr><tr><td align="center">ntbackup</td><td align="center">系统备份和还原</td><td align="center">services.msc</td><td align="center">本地服务设置</td></tr><tr><td align="center">sfc /scannow</td><td align="center">windows文件保护</td><td align="center">taskmgr</td><td align="center">任务管理器</td></tr><tr><td align="center">sysdm.cpl</td><td align="center">系统属性</td><td align="center">msinfo32</td><td align="center">系统信息</td></tr><tr><td align="center">sfc.exe</td><td align="center">系统文件检查器</td><td align="center">tsshutdn</td><td align="center">60秒倒计时关机命令</td></tr><tr><td align="center">shrpubw</td><td align="center">创建共享文件夹</td><td align="center">utilman</td><td align="center">辅助工具管理器</td></tr><tr><td align="center">sigverif</td><td align="center">文件签名验证程序</td><td align="center">wiaacmgr</td><td align="center">扫描仪和照相机向导</td></tr>
    <tr><td align="center">winver</td><td align="center">检查Windows版本</td><td align="center">wmimgmt.msc</td><td align="center">打开windows管理体系结构(WMI)</td></tr><tr><td align="center">write</td><td align="center">写字板</td><td align="center">wscript</td><td align="center">windows脚本宿主设置</td></tr></tbody></table>

<h2 id="内部命令"><a href="#内部命令" class="headerlink" title="内部命令"></a>内部命令</h2><table><thead><tr><th align="center">命令</th><th align="center">描述</th><th align="center">命令</th><th align="center">描述</th></tr></thead><tbody><tr><td align="center">assoc</td><td align="center">显示或修改文件扩展名关联。</td><td align="center">attrib</td><td align="center">显示或更改文件属性。</td></tr><tr><td align="center">break</td><td align="center">设置或清除扩展式CTRL+C检查。</td><td align="center">bootcfg</td><td align="center">设置boot.ini文件的属性以便控制启动加载。</td></tr><tr><td align="center">cacls</td><td align="center">显示或修改文件的访问控制列表(ACL)。</td><td align="center">call</td><td align="center">从另一个批处理程序调用这一个。</td></tr><tr><td align="center">cd</td><td align="center">显示当前目录的名称或将其更改。</td><td align="center">chcp</td><td align="center">显示或设置活动代码页数。</td></tr><tr><td align="center">chdir</td><td align="center">显示当前目录的名称或将其更改。</td><td align="center">color</td><td align="center">设置默认控制台前景和背景颜色。</td></tr><tr><td align="center">chkdsk</td><td align="center">检查磁盘并显示状态报告。</td><td align="center">comp</td><td align="center">比较两个或两套文件的内容。</td></tr><tr><td align="center">chkntfs</td><td align="center">显示或修改启动时间磁盘检查。</td><td align="center">compact</td><td align="center">显示或更改NTFS分区上文件的压缩。</td></tr><tr><td align="center">cls</td><td align="center">清除屏幕。</td><td align="center">convert</td><td align="center">将FAT卷转换成NTFS。您不能转换当前驱动器。</td></tr><tr><td align="center">cmd</td><td align="center">打开另一个Windows命令解释程序窗口。</td><td align="center">copy</td><td align="center">将至少一个文件复制到另一个位置。</td></tr><tr><td align="center">date</td><td align="center">显示或设置日期。</td><td align="center">MOVE</td><td align="center">将一个或多个文件从一个目录移动到另一个目录。</td></tr><tr><td align="center">del</td><td align="center">删除至少一个文件。</td><td align="center">OPENFILES</td><td align="center">显示远程用户为了文件共享而打开的文件。</td></tr><tr><td align="center">dir</td><td align="center">显示一个目录中的文件和子目录。</td><td align="center">PAGEFILECONFIG</td><td align="center">显示或配置页面文件的属性。</td></tr><tr><td align="center">DISKCOMP</td><td align="center">比较两个软盘的内容。</td><td align="center">path</td><td align="center">为可执行文件显示或设置搜索路径。</td></tr><tr><td align="center">DISKCOPY</td><td align="center">将一个软盘的内容复制到另一个软盘。</td><td align="center">pause</td><td align="center">停止批处理文件的处理并显示信息。</td></tr><tr><td align="center">DISKPART</td><td align="center">显示或配置磁盘分区属性。</td><td align="center">POPD</td><td align="center">还原由PUSHD保存的当前目录上一次的值。</td></tr><tr><td align="center">DOSKEY</td><td align="center">编辑命令行、调用Windows命令并创建宏。</td><td align="center">PRINT</td><td align="center">打印一个文本文件。</td></tr><tr><td align="center">DRIVERQUERY</td><td align="center">显示当前设备驱动程序状态和属性。</td><td align="center">PROMPT</td><td align="center">改变Windows命令提示。</td></tr><tr><td align="center">echo</td><td align="center">显示消息，或将命令回显打开或关上。</td><td align="center">PUSHD</td><td align="center">保存当前目录，然后对其进行更改。</td></tr><tr><td align="center">ENDLOCAL</td><td align="center">结束批文件中环境更改的本地化。</td><td align="center">rd</td><td align="center">删除目录。</td></tr><tr><td align="center">ERASE</td><td align="center">删除一个或多个文件。</td><td align="center">RECOVER</td><td align="center">从损坏的磁盘中恢复可读取的信息。</td></tr><tr><td align="center">EVENTQUERY</td><td align="center">显示指定准则的事件日志项目。</td><td align="center">rem</td><td align="center">记录批处理文件或CONFIG.SYS中的注释。</td></tr><tr><td align="center">exit</td><td align="center">退出CMD.EXE程序(命令解释程序)。</td><td align="center">ren</td><td align="center">重新命名文件。</td></tr><tr><td align="center">fc</td><td align="center">比较两个文件或两个文件集并显示它们之间的不同。</td><td align="center">rename</td><td align="center">重新命名文件。</td></tr><tr><td align="center">find</td><td align="center">在一个或多个文件中搜索一个文字字符串。</td><td align="center">REPLACE</td><td align="center">替换文件。</td></tr><tr><td align="center">FINDSTR</td><td align="center">在多个文件中搜索字符串。</td><td align="center">rmdir</td><td align="center">删除目录。</td></tr><tr><td align="center">for</td><td align="center">为一套文件中的每个文件运行一个指定的命令。</td><td align="center">set</td><td align="center">显示、设置或删除Windows环境变量。</td></tr><tr><td align="center">FORMAT</td><td align="center">格式化磁盘，以便跟Windows使用。</td><td align="center">SETLOCAL</td><td align="center">开始用批文件改变环境的本地化。</td></tr><tr><td align="center">FSUTIL</td><td align="center">显示或配置文件系统的属性。</td><td align="center">sc</td><td align="center">显示或配置服务(后台处理)。</td></tr><tr><td align="center">ftype</td><td align="center">显示或修改用在文件扩展名关联的文件类型。</td><td align="center">SCHTASKS</td><td align="center">安排命令和程序在一部计算机上按计划运行。</td></tr><tr><td align="center">goto</td><td align="center">将Windows命令解释程序指向批处理程序</td><td align="center">SHIFT</td><td align="center">调整批处理文件中可替换参数的位置。</td></tr><tr><td align="center">GPRESULT</td><td align="center">显示机器或用户的组策略信息。</td><td align="center">SHUTDOWN</td><td align="center">让机器在本地或远程正确关闭。</td></tr><tr><td align="center">GRAFTABL</td><td align="center">启用Windows在图形模式显示扩展字符集。</td><td align="center">sort</td><td align="center">将输入排序。</td></tr><tr><td align="center">help</td><td align="center">提供Windows命令的帮助信息。</td><td align="center">start</td><td align="center">打开单独视窗运行指定程序或命令。</td></tr><tr><td align="center">if</td><td align="center">在批处理程序中执行有条件的处理过程。</td><td align="center">subst</td><td align="center">将驱动器号与路径关联。</td></tr><tr><td align="center">label</td><td align="center">创建、更改或删除磁盘的卷标。</td><td align="center">SYSTEMINFO</td><td align="center">显示机器的具体的属性和配置。</td></tr><tr><td align="center">md</td><td align="center">创建一个目录。</td><td align="center">tasklist</td><td align="center">显示包括服务的所有当前运行的任务。</td></tr><tr><td align="center">mkdir</td><td align="center">创建一个目录。</td><td align="center">taskkill</td><td align="center">终止正在运行的进程或应用程序。</td></tr><tr><td align="center">mode</td><td align="center">配置系统设备。</td><td align="center">time</td><td align="center">显示或设置系统时间。</td></tr><tr><td align="center">more</td><td align="center">逐屏显示输出。</td><td align="center">title</td><td align="center">设置CMD.EXE会话的窗口标题。</td></tr><tr><td align="center">wmic</td><td align="center">在交互命令外壳里显示WMI信息。</td><td align="center">verify</td><td align="center">告诉Windows验证文件是否正确写入磁盘。</td></tr><tr><td align="center">xcopy</td><td align="center">复制文件和目录树。</td><td align="center">ver</td><td align="center">显示Windows的版本。</td></tr><tr><td align="center">vol</td><td align="center">显示磁盘卷标和序列号。</td><td align="center">type</td><td align="center">显示文本文件的内容。</td></tr><tr><td align="center">tree</td><td align="center">以图形显示启动器或路径的目录结构。</td><td align="center">ping</td><td align="center">网络命令</td></tr></tbody></table>]]></content>
      <categories>
        <category>Windows</category>
      </categories>
      <tags>
        <tag>命令行</tag>
      </tags>
  </entry>
</search>
